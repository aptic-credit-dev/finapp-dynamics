# schema: finapp-dynamics/implementation-manifest/v1
project: Finapp Dynamics
description: >
  Machine-readable implementation plan for the Finapp Dynamics enterprise SaaS platform. This manifest is the
  authoritative build plan for the repository. Stage 0 (toolchain) is implemented and tested; every business
  module is still design-only — an extensive in-session REFERENCE implementation informs the specs, but no
  business code exists in the target repo. Statuses: documented | approved_for_build | implemented | deferred |
  requires_review.
status_legend:
  documented: Specified and consolidated; not yet approved for build in the target repo.
  approved_for_build: Cleared to build next, in dependency order.
  implemented: Real, tested code exists in this repo. Never set without passing tests.
  deferred: Designed but intentionally later (post-MVP / commercial / Phase 7).
  requires_review: Needs a business/security/engineering decision before build (see OPEN_QUESTIONS.md).

conventions:
  language: TypeScript (NestJS) + PostgreSQL 16
  isolation: RLS FORCE + tenant_isolation policy + composite (tenant_id, id) keys + composite FKs
  events: single transactional outbox (m06); typed event union in contracts
  audit: single registry (m03); registered SCREAMING_SNAKE codes; append-only
  money: decimal-safe (minor units / exact decimal); no float
  api: /api/v1/*; audited endpoints with permission + audit code
  ships_with_every_module: [permissions, events, audit_codes, tests, docs_update, manifest_update]

stages:
  - stage: 0
    name: Repository & toolchain foundation
    status: implemented
    priority: P0
    mvp: true
    target_location: repo root, packages/kernel, packages/contracts, apps/api, apps/web, tools/
    description: Scaffold monorepo, TS project refs, migration runner, CI skeleton, test harness. No business logic.
    dependencies: []
    shared_services_consumed: []
    testing_required: [smoke_lane_skeleton, db_lane_skeleton]
    delivered:
      monorepo: npm workspaces — packages/{kernel,contracts}, apps/{api,web}, tools/{migrate,test-runner}
      module_placeholders: packages/<module>/README.md for all 43 business modules (no code)
      typescript: project references, strict + noUncheckedIndexedAccess + exactOptionalPropertyTypes
      kernel: DI tokens (DB, AUDIT, AUTHZ, OUTBOX), RequestContext/SystemContext, ProblemError (RFC 9457),
        Endpoint decorator (permission + auditCode), ambient-transaction Db (withTenant/withSystem)
      contracts: DomainEvent union — deliberately empty (`never`) at Stage 0
      migration_runner: ordered by module (dependency order), checksummed + immutable, idempotent,
        atomic per migration, advisory-locked
      test_harness: framework-free PURE runner (node --experimental-strip-types, runs off source with no
        build) + DB integration lane (skipped without DATABASE_URL)
      ci: .github/workflows/ci.yml — smoke lane on PR, DB lane (postgres:16) on push
    verification:
      build: clean
      lint: clean
      format: clean
      smoke_lane: 3 suites / 73 assertions passing
      db_lane: 1 spec / 26 assertions passing — RLS convention proven, sample tables removed
      api_boot: verified — GET /api/v1/health 200; unrouted paths 404 as application/problem+json
      caveat: >
        The RLS proof was executed locally against PostgreSQL 15.2 (the only server available on the
        build machine); the platform targets PostgreSQL 16 and CI runs postgres:16. RLS FORCE, the
        tenant_isolation policy, and composite FK semantics are identical across 15 and 16, but the
        first CI DB-lane run on 16 is the authoritative confirmation.
    findings:
      - The tenant_isolation policy REQUIRES NULLIF(current_setting('app.tenant_id', true), ''). A
        transaction-local GUC reverts to the empty string, not NULL, so on a pooled connection the
        policy would raise `invalid input syntax for type uuid` instead of matching zero rows. Found by
        the db-spec; the convention and the sample were corrected.
      - RLS FORCE does not constrain a SUPERUSER — only the table owner. The app and owner roles are
        therefore both non-superuser and NOBYPASSRLS. Asserted explicitly in the db-spec.
    not_included:
      - No business tables, routes, permissions, events, or audit codes (by design).
      - Kernel tokens are declared but unbound — their owning modules land in Stage 1.
      - Boot-time validation of @Endpoint against naming-map.yaml + audit-code-registry.yaml lands with
        the first module that declares a route (Stage 1); there is nothing to validate until then.
      - apps/web is a framework-free shell — the frontend stack is undecided (OPEN_QUESTIONS.md #17).

  - stage: 1
    name: SaaS foundation
    status: approved_for_build
    priority: P0
    mvp: true
    depends_on_stage: 0
    modules:
      - module: m01-tenant
        capability: Tenancy control plane + org hierarchy + provisioning
        status: implemented
        implemented_in: Stage 1A
        merged: {pr: 2, merge_commit: 251decf, base: main}
        certified:
          postgres_version: 16
          evidence: "CI run 29417746720 (push, main) and 29417651716 (pull_request, PR #2) — DB lane PASS;
            'Assert PostgreSQL 16' asserted server_version_num is 16.x; 'DB integration specs' passed; no step skipped."
          note: >
            Developed against local PostgreSQL 15.2; the CI runs above are the authoritative certification.
            The Stage 1A caveat is discharged.
        delivered:
          tables: [tenant_type_catalogue(global reference), tenants(control plane, RLS FORCE + system escape per ADR-014),
                   tenant_status_history(append-only), tenant_environments, tenant_entities, tenant_departments, tenant_branches]
          api: /api/v1/tenants — registry CRUD + 11 lifecycle actions + status history
          permissions: 19 under tenant.* (declared, NOT granted — no role model until m02)
          audit_codes: 18 under TENANT_ (all registered in audit-code-registry.yaml)
          events: tenant.lifecycle family, 17 types, payload v1 (GAP-1 closed)
          tests: PURE smoke (251 assertions) + DB spec (46 assertions)
        # Each entry is a QUOTED scalar. Unquoted, a line like `- ADR-014: tenants is ...` is a YAML map
        # key, and the continuation line under it is a parse error — which is exactly how this file was
        # broken between Stage 1A and Stage 1B. tools/conformance now parses every manifest in the smoke
        # lane, so it cannot happen silently again.
        deviations:
          - "ADR-014: tenants is RLS FORCE with an explicit system escape; org tables are tenant-scoped
             FORCE. Both are STRICTER than ADR-001/SAAS_FOUNDATION, which permit them to be global
             non-FORCE."
          - "Permissions are tenant.<entity>.<action> (e.g. tenant.registry.view) because the kernel's
             @Endpoint validator requires three segments; audit codes are TENANT_<ENTITY>_<ACTION> because
             the audit registry format requires three segments. The two-segment names in the Stage 1A
             prompt are invalid under the repository's own conventions."
          - "API prefix is /api/v1/tenants per naming-map.yaml, not /api/v1/admin/tenants."
        not_included:
          - "No authentication: the API reads actor + permissions from headers. m02-identity replaces
             this."
          - "AUTHZ/AUDIT/OUTBOX are bound to clearly-marked Stage 1A stand-ins
             (m01-tenant/src/adapters.ts). Audit is not persisted to a spine; events are not durably
             queued. No second outbox table exists."
          - "No down-migrations: the Stage 0 migration runner does not support them."
        mvp: true
        db_ownership: [tenants(global), subsidiaries(global), subscription_plans(global), tenant_subscriptions(global), usage_meters(global), department, branch]
        api_ownership: [/api/v1/tenants]
        event_ownership: [tenant.lifecycle]
        shared_services_consumed: [kernel]
        permissions_required: [tenant.*]
        testing_required: [smoke, db_spec_isolation]
      - module: m02-identity
        capability: Authentication + RBAC + permission catalogue + SoD
        status: documented
        # SPLIT APPROVED 2026-07-15 (product owner) — planning only, no code yet.
        # This row's capability spans identity + authentication + RBAC, which is one very large stage and
        # conflicts with the Stage 1B prompt's exclusion list. Resolved by splitting into three sub-stages.
        # Recorded here so the manifest and the build do not disagree; see
        # docs/build/stages/STAGE_1B_M02_IDENTITY_READINESS.md §10.
        substages:
          # 1B IMPLEMENTED 2026-07-15. Domain, migrations, services, actor resolution, the API boundary and
          # all three route families are built and tested; x-actor-id is deleted and a conformance check
          # forbids its return. See docs/build/stages/STAGE_1B_M02_IDENTITY_COMPLETION.md for the gate
          # decision and the honest list of what 1B does NOT do.
          - {substage: 1B, module: m02-identity, status: implemented,
             owns: [identities(global), user_accounts(global), tenant_memberships,
                    identity_status_history, account_status_history, membership_status_history,
                    authentication_subjects(references only),
                    identity_type_catalogue, account_type_catalogue, membership_type_catalogue],
             # The original row said /api/v1/users for the whole of identity. Collapsing identities +
             # accounts + memberships behind one prefix would be the "one generic user service" the
             # architecture forbids — they are distinct resources with distinct lifecycles, permissions and
             # scopes. /api/v1/users stays reserved for 1C. Registered in naming-map.yaml.
             api: [/api/v1/identities, /api/v1/accounts, /api/v1/tenant-memberships],
             events: [identity.lifecycle], permissions: ["identity.*"], audit: [IDENTITY_],
             ends_with: "x-actor-id deleted; actor enters RequestContext via a signed, environment-gated dev adapter"}
          # 1C IMPLEMENTED 2026-07-18 on feature/stage-1c-authentication-sessions (NOT yet merged/certified).
          # Opaque revocable server-side sessions (ADR-015) + Argon2id credentials (ADR-016); Secure HttpOnly
          # SameSite cookies with CSRF. The Stage 1B dev actor assertion (x-dev-actor) is DELETED and a
          # conformance check forbids its return. x-permissions + ContextAuthz remain, untouched, for 1D.
          - {substage: 1C, module: m02-auth, status: implemented,
             owns: [authentication_credentials(global), login_attempts(global, pre-auth), sessions(global),
                    session_status_history(global, append-only), session_refresh_tokens(global)],
             api: [/api/v1/auth], events: [identity.authentication], permissions: ["auth.*"], audit: [AUTH_],
             ends_with: "x-dev-actor deleted; real session-backed actor via SessionActorAdapter"}
          - {substage: 1D, module: m02-rbac, status: implemented,
             owns: [permissions(global reference), roles(mixed), role_permissions(mixed),
                    role_assignments(tenant), platform_role_assignments(global),
                    sod_rules(mixed), role_status_history(mixed, append-only),
                    assignment_status_history(mixed, append-only)],
             api: [/api/v1/rbac], events: [identity.authorization], permissions: ["rbac.*"], audit: [RBAC_],
             ends_with: "x-permissions DELETED; ContextAuthz DELETED and AUTHZ bound to RbacAuthz; permissions
                         resolved from persistent role assignments, fresh per request"}
        # -------------------------------------------------------------------------------------------------
        # SUBSTAGE 1B CERTIFICATION STATUS (recorded 2026-07-17).
        #
        # `status: implemented` on the 1B substage row above is the closest LEGEND value — the conformance
        # suite requires status to be one of documented|approved_for_build|implemented|deferred|
        # requires_review, and `implemented` is accurate: real, tested code exists (asserted by conformance,
        # which checks m02 ships the actor resolver when marked implemented). It does NOT by itself claim
        # merged or PostgreSQL-16-certified. The keys below carry that distinction and sit OUTSIDE any
        # `status` field so the legend check is unaffected. Field mapping to the requested status model:
        #   implementation_status: implemented_on_feature_branch   (== legend `implemented`)
        #   certification_status:  pending_postgresql_16_ci
        #   merge_status:          not_merged
        #   stage_gate:            conditional_go
        certification:
          module: m02-identity
          substage: 1B
          branch: feature/stage-1b-m02-identity
          remote_head: 586faee   # feature-branch head at PR time; merged as 9baf98c via PR #3
          # STATUS MODEL (mapped to fields; the substage `status: implemented` above stays — it is the
          # closest legend value and real tested code exists and is now on main):
          #   implementation_status:        implemented   (merged to main via PR #3)
          #   postgresql_16_certification:  passed         (DB lane green on the PR and post-merge run)
          #   smoke_certification:          pending_remediation  (Smoke lane failed at Lint)
          #   merge_status:                 merged_with_failed_required_lane
          #   stage_gate:                   no_go_pending_remediation
          implementation_status: implemented
          postgresql_16_certification: passed
          smoke_certification: pending_remediation
          merge_status: merged_with_failed_required_lane
          stage_gate: no_go_pending_remediation
          stage_1c_readiness: no_go_until_smoke_remediation_merged
          merged_pr: 3
          merge_commit: a94c0ab
          remediation:
            branch: fix/stage-1b-ci-remediation
            reason: "PR #3 merged with a RED required Smoke lane (Lint failed; Build + smoke skipped)"
            failed_run: 29575596082      # pull_request run for PR #3 head 9baf98c
            post_merge_failed_run: 29575701016   # push run on main a94c0ab, same failure
            root_cause: "apps/api excluded test/** from its build tsconfig; the integration spec was
                         type-aware-linted via a classic-`project` eslint tsconfig that resolved @finapp/*
                         types through BUILT dist. CI lints before building, so dist was absent and lint
                         produced 173 unresolved-type errors."
            fix: "include apps/api test/** in its tsconfig + reference test-runner (uniform with all
                  workspaces); delete tsconfig.eslint.json and the eslint override; declare the Nest app
                  surface the spec drives (3 latent TS2352 casts). projectService now resolves to source and
                  lint needs no prior build."
            regression_guard: "conformance asserts every workspace with a test/ dir includes it in tsconfig"
            local_reverification: {smoke_suites: 7, smoke_assertions: 926, db_specs: 5, db_assertions: 254,
                                   fresh_checkout: true, postgres_version: "15.2"}
          pull_request: merged_pr_3_then_remediation_pr_pending
          postgres16_ci: passed   # DB lane green on run 29575596082 and 29575701016
          local_verification:
            smoke_suites: 7
            smoke_assertions: 926   # +7 conformance assertions from the regression guard
            db_specs: 5
            db_assertions: 254
            postgres_version: "15.2"   # local only; PostgreSQL 16 in CI is the authoritative gate
          scope_implemented: [identity_registry, user_account_registry, tenant_membership,
                              identity_account_membership_lifecycles, actor_resolver, dev_signed_actor_adapter,
                              identity_api, account_api, tenant_membership_api, m01_actor_context_integration,
                              x_actor_id_trust_removed, conformance_protections]
          deferred_boundaries:
            - "dev identity assertion + authentication/sessions -> Stage 1C"
            - "x-permissions removed -> Stage 1D"
            - "ContextAuthz removed -> Stage 1D"
            - "persistent RBAC -> Stage 1D"
            - "audit persistence -> m03 (deferred)"
            - "outbox delivery -> m06 (deferred)"
          conditions:
            - {id: C-CI, name: PostgreSQL 16 certification, owner: repository_maintainer,
               action: "open the Stage 1B pull request; confirm the PostgreSQL 16 DB lane passes with all
                        expected M01 and M02 specs discovered and no skipped database steps",
               merge_restriction: "Stage 1B must not merge until green",
               evidence_required: "GitHub Actions run id, server_version_num assertion, passing test counts"}
            - {id: C1, name: PAT revocation, owner: repository_or_org_admin,
               action: "revoke the previously exposed GitHub Personal Access Token",
               merge_restriction: "treat as unresolved security exposure until confirmed",
               evidence_required: "confirmation from the token owner or GitHub settings"}
            - {id: C2, name: main branch protection, owner: repository_admin,
               action: "protect main and require the Smoke and PostgreSQL 16 checks before merge",
               merge_restriction: "should not merge until protection is active unless a formally approved
                                   exception is documented",
               evidence_required: "GitHub branch-protection status"}
            - {id: C3, name: repository visibility, owner: repository_owner,
               action: "confirm whether public visibility is intentional or make the repository private",
               merge_restriction: "apply the organization's approved security decision",
               evidence_required: "repository visibility status and owner decision"}
          docs: docs/build/stages/STAGE_1B_M02_IDENTITY_COMPLETION.md
        # -------------------------------------------------------------------------------------------------
        # SUBSTAGE 1C CERTIFICATION STATUS (recorded 2026-07-18).
        #   implementation_status:        implemented   (substages[1] status: implemented, above)
        #   merge_status:                 merged         (PR #5)
        #   smoke_certification:          passed
        #   postgresql_16_certification:  passed
        #   stage_gate:                   go
        #   status:                       certified_and_complete
        certification_1c:
          module: m02-auth
          substage: 1C
          implementation_status: implemented
          smoke_certification: passed
          postgresql_16_certification: passed
          merge_status: merged
          stage_gate: go
          status: certified_and_complete
          stage_1d_readiness: go   # 1C certified on main; 1D (m02-rbac) may begin from 48cba39. NOT started.
          branch_protection: active
          merged_pr: 5
          feature_head: 8d1eeac
          merge_commit: 48cba39fe59b19c03648623b1dbd92ac4a70017c   # main HEAD, squash of PR #5
          parent_baseline: e3e51a5   # certified Stage 1B
          certified_ci_run: 29646472150   # PR #5 — Smoke + DB (Assert PostgreSQL 16) both success, no skips
          post_merge_ci_run: 29653121124  # push run on main 48cba39 — success
          local_reverification: {smoke_suites: 8, smoke_assertions: 1004, db_specs: 7, db_assertions: 301,
                                  postgres_version: "15.2", clean_checkout: true, tree_identical_to_ci_head: true}
          docs: docs/build/stages/STAGE_1C_AUTH_SESSIONS_COMPLETION.md
        # -------------------------------------------------------------------------------------------------
        # SUBSTAGE 1D STATUS (recorded 2026-07-19). Honest, and deliberately NOT claiming certified or merged.
        # The substage `status: implemented` above is the closest LEGEND value and is accurate: real, tested
        # code exists (m02-rbac module, RBAC API, bootstrap; smoke + DB specs green locally). The keys below
        # carry the merge/certification distinction and sit OUTSIDE any `status` field.
        certification_1d:
          module: m02-rbac
          substage: 1D
          branch: feature/stage-1d-rbac-authorization
          certification_branch: cert/stage-1d-rbac-authorization
          parent_baseline: 004b2fd   # certified Stage 1C merge on main; NOT rebased onto anything uncertified
          feature_head: 4669cca      # reviewed head of PR #7 (CI formatting fix)
          # Post-merge certification (recorded 2026-07-22). Squash-merged: beea0d9 has single parent 004b2fd,
          # so 4669cca is represented by content (tree byte-identical across all Stage 1D paths), not ancestry.
          implementation_status: merged
          implementation_pr: 7
          implementation_head_sha: 4669cca45e27a177dd1e63790058da6cb2cb527e
          implementation_merge_sha: beea0d90dd283b1289a74af0107b3751794dd245
          smoke_certification: passed_locally          # 9 suites, 1166 assertions, 0 failures
          postgresql_certification: passed_locally      # 9 db specs, 332 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                      # PR #7 run 29898123814 on 4669cca: Smoke + DB (PostgreSQL 16) both green
          certification_status: certified_on_branch
          certified_baseline_sha: beea0d90dd283b1289a74af0107b3751794dd245   # actual main SHA tested
          certification_pr: pending                     # cert PR prepared, NOT merged
          merge_status: merged
          stage_gate: go                                # architecture accepted (ADR-017..020)
          stage_2_readiness: not_started                # Stage 2 must NOT begin until the certification PR merges
          local_reverification: {smoke_suites: 9, smoke_assertions: 1166, db_specs: 9, db_assertions: 332,
                                  postgres_version: "15.2", clean_checkout: true, tree_identical_to_ci_head: true}
          docs: docs/build/stages/STAGE_1D_RBAC_AUTHORIZATION_COMPLETION.md
          certification_docs: docs/build/stages/STAGE_1D_RBAC_AUTHORIZATION_CERTIFICATION.md
        # -------------------------------------------------------------------------------------------------
        open_gaps:
          - "GAP-1 CLOSED in 1B: identity.lifecycle is registered in event-registry.yaml, declared in the
             contracts union, and naming-map event_family_registered is true. The conformance suite now
             asserts the agreement, so the two files cannot silently diverge again."
        blocks_removal_of:
          - "packages/m01-tenant/src/adapters.ts ContextAuthz — RESOLVED in 1D. The class is DELETED and
             AUTHZ is bound to the persistent RbacAuthz (apps/api/src/platform.module.ts). The x-permissions
             header is gone from all live source; the conformance suite now asserts ZERO live use of both
             x-permissions and ContextAuthz, so neither can return quietly."
          - "packages/m02-identity/src/dev-actor-adapter.ts DevActorAdapter — the signed development actor
             source. MUST NOT outlive substage 1C. It is not authentication: it proves the caller holds the
             dev secret, never that they are the person, so assurance is 'development' and never better.
             It refuses to construct outside NODE_ENV=development|test, and apps/api refuses to BOOT in
             production rather than serve without an actor source. The x-dev-actor header is named in
             exactly ONE file and the conformance suite asserts that."
        mvp: true
        # Whole-module ownership, across all three substages. The 1B slice is the substages[0] row above;
        # roles/permissions/user_roles/sessions/login_attempts remain UNBUILT and are asserted absent by
        # the conformance suite, so 1B cannot have quietly reached into 1C or 1D.
        db_ownership: [identities(global), user_accounts(global), tenant_memberships,
                       authentication_subjects(global), roles, permissions, user_roles,
                       login_attempts(global), sessions]
        api_ownership: [/api/v1/identities, /api/v1/accounts, /api/v1/tenant-memberships,
                        /api/v1/auth, /api/v1/users, /api/v1/roles]
        event_ownership: [identity.lifecycle]
        shared_services_consumed: [kernel, m01, m03]
        permissions_required: [identity.*, rbac.*]
        testing_required: [smoke, db_spec_authz]
      - module: m03-audit
        capability: Audit spine + code registry (append-only, tamper-evident)
        status: implemented   # Stage 2.1 — implemented_on_feature_branch, rebased onto certified main; see certification_2_1
        mvp: true
        db_ownership: [audit_events(mixed, append-only, hash-chained),
                       audit_retention_policy(mixed), audit_legal_hold(tenant)]
        api_ownership: [/api/v1/audit]
        event_ownership: []
        shared_services_consumed: [kernel, m01, m02]
        permissions_required: [audit.event.view, audit.event.search, audit.event.export,
                               audit.platform.view, audit.integrity.verify, audit.retention.manage,
                               audit.configuration.manage]
        testing_required: [smoke, db_spec_audit_chain]
        # Deferred (documented, ADR-029/031): monthly partitioning; DB-backed audit_code_registry table (the
        # YAML registry remains authoritative); chain_anchors external anchoring; retention-enforcement worker;
        # operational-metrics endpoints. Note db_ownership above reflects what SHIPPED, not the reference list.
        certification_2_1:
          module: m03-audit
          substage: "2.1"
          branch: feature/stage-2-1-m03-audit
          certification_branch: cert/stage-2-1-m03-audit
          # Merged 2026-07-22 via PR #9 (squash) onto certified Stage 1D main. Post-merge certification recorded.
          parent_baseline: 56b7d3ea9cba42a81685fb6aefa1735fe8d314b3   # certified Stage 1D main (PR #8 merge)
          implementation_status: merged
          implementation_pr: 9
          implementation_head: f6dd8e9af8385778689533131898f3332a033d64
          implementation_merge_sha: 587a3ce6afdbcd3d4d7e3735117c4e7220568a0e
          smoke_certification: passed_locally           # 10 suites, 1256 assertions, 0 failures
          postgresql_certification: passed_locally      # 10 db specs, 356 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                       # PR #9 run 29909503769 on f6dd8e9: Smoke + DB (PostgreSQL 16) both green
          certification_status: certified               # cert PR #11 merged 2026-07-23
          certified_baseline_sha: 587a3ce6afdbcd3d4d7e3735117c4e7220568a0e   # merged main SHA tested for certification
          certification_pr: 11
          certification_merge_sha: cd29b7b5b1c0220fd801989e528d06911395b7a9   # PR #11 squash merge
          merge_status: merged
          adrs: [ADR-029, ADR-030, ADR-031]
          local_reverification: {smoke_suites: 10, smoke_assertions: 1256, db_specs: 10, db_assertions: 356,
                                  postgres_version: "15.2", clean_checkout: true, tree_identical_to_ci_head: true}
          docs: docs/build/stages/STAGE_2_1_M03_AUDIT_COMPLETION.md
          certification_docs: docs/build/stages/STAGE_2_1_M03_AUDIT_CERTIFICATION.md
      - module: m06-workflow
        capability: Status, workflow, SLA, timeline, transactional outbox, idempotency
        status: implemented   # Stage 2.2 — implemented_on_branch (not merged/certified); see certification_2_2
        mvp: true
        db_ownership: [workflow_definition, workflow_definition_version, workflow_instance, workflow_token,
                       workflow_task, workflow_timer, workflow_sla_clock, workflow_incident,
                       workflow_event_outbox(mixed, THE outbox), workflow_instance_history(append-only),
                       workflow_task_history(append-only)]
        api_ownership: [/api/v1/workflow]
        event_ownership: [workflow.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03]
        permissions_required: [workflow.*]
        testing_required: [smoke, db_spec_outbox_idempotency]
        # Deviation (documented): a definition's graph is stored as one validated, immutable `spec` JSON on
        # workflow_definition_version (ADR-022) rather than shredded into per-node tables; entity_type_registry
        # NOT created (OD-4). SUB_WORKFLOW/COMPENSATION reserved but deferred. See completion report.
        certification_2_2:
          module: m06-workflow
          substage: "2.2"
          branch: feature/stage-2-2-m06-workflow
          certification_branch: cert/stage-2-2-m06-workflow
          parent_baseline: cd29b7b5b1c0220fd801989e528d06911395b7a9   # certified Stage 2.1 main (PR #11)
          # Merged 2026-07-24 via PR #12 (squash) onto certified Stage 2.1 main. Post-merge certification recorded.
          implementation_status: merged
          implementation_pr: 12
          implementation_head: 7ade813d5e41d393f6dad5c4fef83d5153aa09af
          implementation_merge_sha: 3823275d11e22a400b87fbdc42b58b6b9ee49a10
          merge_status: merged
          ci_implementation: passed                     # PR #12 run 30085639355 on 7ade813: Smoke + DB (PostgreSQL 16) both green
          smoke_certification: passed_locally           # 12 suites, 1643 assertions, 0 failures
          postgresql_certification: passed_locally      # 13 db specs, 408 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                       # same authoritative run 30085639355 (byte-identical merged tree)
          certification_status: certified               # cert PR #13 merged 2026-07-24
          certified_baseline_sha: 3823275d11e22a400b87fbdc42b58b6b9ee49a10   # merged main SHA tested for certification
          certification_pr: 13
          certification_merge_sha: 130c28447a8e6f548b2f3048fdb9a0504470a2cd
          local_reverification: {smoke_suites: 12, smoke_assertions: 1643, db_specs: 13, db_assertions: 408,
                                  postgres_version: "15.2", node_version: "22.14.0", clean_checkout: true, tree_identical_to_ci_head: true}
          adrs: [ADR-021, ADR-022, ADR-023, ADR-024, ADR-025, ADR-026]
          docs: docs/build/stages/STAGE_2_2_M06_WORKFLOW_COMPLETION.md
          certification_docs: docs/build/stages/STAGE_2_2_M06_WORKFLOW_CERTIFICATION.md
      - module: m07-rules
        capability: Versioned explainable rules + decision tables
        status: implemented   # Stage 2.3 — implemented_on_branch (not merged/certified); see certification_2_3
        mvp: true
        db_ownership: [rule_set, rule_set_version(immutable spec jsonb, one ACTIVE per set),
                       rule_evaluation(append-only, input-hash not raw), rule_test_case,
                       rule_set_history(append-only)]
        api_ownership: [/api/v1/rules]
        event_ownership: [rules.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06]
        permissions_required: [rules.*]
        testing_required: [smoke, db_spec, services_db_spec, api_http_spec]
        tests_present: [m07-rules.smoke (156), m07-rules.db-spec (22), m07-services.db-spec (25),
                        api-rules.db-spec (18)]
        # Design (ADR-032/033): rule graph stored as one immutable validated `spec` JSON on rule_set_version;
        # conditions are STRUCTURED typed JSON (no free-text host-code interpreter) and decimal-safe. Evaluation
        # evidence stores the input HASH, never raw sensitive inputs (ADR-035). Global rule scope modelled but
        # global-admin surface minimal (ADR-037). m06 integration is a stable contract (ADR-036).
        certification_2_3:
          module: m07-rules
          substage: "2.3"
          branch: feature/stage-2-3-m07-rules
          certification_branch: cert/stage-2-3-m07-rules
          parent_baseline: 130c28447a8e6f548b2f3048fdb9a0504470a2cd   # certified Stage 2.2 main (PR #13 merge)
          # Merged 2026-07-26 via PR #14 (squash) onto certified Stage 2.2 main. Post-merge certification recorded on branch.
          implementation_status: merged
          implementation_pr: 14
          implementation_head: 6c16aa301eff8d4ca885b181c384aa66e2c89299
          implementation_merge_sha: 0701a011b1fdf160e8348346f74aac3440cf32a9
          merge_status: merged
          ci_implementation: passed                     # PR #14 run 30152364016 on 6c16aa3: Smoke + DB (PostgreSQL 16) both green
          smoke_certification: passed_locally           # 13 suites, 1869 assertions, 0 failures
          postgresql_certification: passed_locally      # 16 db specs, 473 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                       # cert PR #15 run 30201770623 + main push 30202145284 (PostgreSQL 16) all green
          certification_status: certified                # cert PR #15 merged 2026-07-26
          certified_baseline_sha: f5b06d7dda4619c7062a89c52ac2ee17a3e494f6   # merged main SHA (PR #15) tested for certification
          certification_pr: 15
          certification_merge_sha: f5b06d7dda4619c7062a89c52ac2ee17a3e494f6
          local_reverification: {smoke_suites: 13, smoke_assertions: 1869, db_specs: 16, db_assertions: 473,
                                  postgres_version: "15.2", node_version: "22.14.0", clean_checkout: true, tree_identical_to_ci_head: true}
          adrs: [ADR-032, ADR-033, ADR-034, ADR-035, ADR-036, ADR-037]
          docs: [docs/build/stages/STAGE_2_3_M07_RULES_COMPLETION.md,
                 docs/build/stages/STAGE_2_3_M07_RULES_CERTIFICATION.md]
      - module: m08-notify
        capability: Notifications + escalation
        status: implemented   # Stage 2.4 — implemented_on_branch (not merged/certified); see certification_2_4
        mvp: true
        db_ownership: [notification_template, notification_template_version(immutable spec jsonb, one ACTIVE per template),
                       notification_request(lease + idempotency), notification_delivery(append-only evidence),
                       escalation_policy(immutable spec, one ACTIVE per key), escalation_instance(lease),
                       notification_preference(user prefs + destination suppression), inbox_notification]
        api_ownership: [/api/v1/notifications]
        event_ownership: [notification.lifecycle]
        shared_services_consumed: [kernel, m01, m03, m06]
        permissions_required: [notifications.*]
        testing_required: [smoke, db_spec, services_db_spec, api_http_spec]
        tests_present: [m08-notify.smoke (85), m08-notify.db-spec (23), m08-services.db-spec (36),
                        api-notify.db-spec (20)]
        # Generic multi-tenant notifications + escalation. Channel-neutral core (email/sms/in_app/webhook ports,
        # deterministic test doubles only — no real provider, no secrets). Safe deterministic template rendering
        # (no eval/Function/vm; escaping + hard limits). Versioned immutable templates + escalation policies.
        # Lease-based worker-safe dispatch + retry + escalation advancement. Preferences/suppression with
        # mandatory-category bypass. In-app inbox. Publishes notification.lifecycle through the ONE m06 outbox.
        certification_2_4:
          module: m08-notify
          substage: "2.4"
          branch: feature/stage-2-4-m08-notifications
          certification_branch: cert/stage-2-4-m08-notifications
          parent_baseline: f5b06d7dda4619c7062a89c52ac2ee17a3e494f6   # certified Stage 2.3 main (PR #15 merge)
          # Merged 2026-07-26 via PR #16 (squash) onto certified Stage 2.3 main. Post-merge certification recorded on branch.
          implementation_status: merged
          implementation_pr: 16
          implementation_head: caebe7e62913b4f299f150e4c8f91e73bc7c0c74
          implementation_merge_sha: f7a8c6b1f909c2e5602f1cc6c60de68d5f69f81d
          merge_status: merged
          ci_implementation: passed                     # PR #16 run 30204816392 on caebe7e: Smoke + DB (PostgreSQL 16) both green
          smoke_certification: passed_locally           # 14 suites, 2066 assertions, 0 failures
          postgresql_certification: passed_locally      # 19 db specs, 552 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                       # cert PR #17 run 30205553439 + main push 30205733810 (PostgreSQL 16) all green
          certification_status: certified                # cert PR #17 merged 2026-07-26
          certified_baseline_sha: 30b69c2534f7ce224d278c73b3872019f5472ac9   # merged main SHA (PR #17) tested for certification
          certification_pr: 17
          certification_merge_sha: 30b69c2534f7ce224d278c73b3872019f5472ac9
          local_reverification: {smoke_suites: 14, smoke_assertions: 2066, conformance_assertions: 708,
                                 db_specs: 19, db_assertions: 552, migrations: 16, postgres_version: "15.2",
                                 node_version: "22.14.0", clean_checkout: true, tree_identical_to_ci_head: true}
          adrs: [ADR-038, ADR-039, ADR-040, ADR-041, ADR-042, ADR-043]
          docs: [docs/build/stages/STAGE_2_4_M08_NOTIFICATIONS_ARCHITECTURE.md,
                 docs/build/stages/STAGE_2_4_M08_NOTIFICATIONS_READINESS.md,
                 docs/build/stages/STAGE_2_4_M08_NOTIFICATIONS_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_2_4_M08_NOTIFICATIONS_COMPLETION.md,
                 docs/build/stages/STAGE_2_4_M08_NOTIFICATIONS_CERTIFICATION.md]
      - module: m09-docs
        capability: Document service (classified, linked, versioned)
        status: implemented   # Stage 2.5 — implemented_on_branch (not merged/certified); see certification_2_5
        mvp: true
        db_ownership: [document_type(immutable spec, one ACTIVE per code), retention_policy(immutable spec),
                       document(record + typed metadata jsonb + legal_hold + disposition state),
                       document_version(immutable, one ACTIVE, committed-frozen, storage ref only),
                       document_access_grant(ACL supplements RBAC), document_checkout(single-winner lease),
                       document_relationship(acyclic), document_legal_hold, document_disposition(evidence),
                       document_scan_result(append-only)]
        api_ownership: [/api/v1/documents]
        event_ownership: [document.lifecycle]
        shared_services_consumed: [kernel, m01, m03, m06]
        permissions_required: [documents.*]
        testing_required: [smoke, db_spec, services_db_spec, api_http_spec]
        tests_present: [m09-docs.smoke (69), m09-docs.db-spec (26), m09-services.db-spec (36),
                        api-documents.db-spec (12)]
        # Enterprise document & records management. Bytes in an object store behind a storage PORT (never in PG;
        # opaque storage ref only, ADR-044); immutable versions (one ACTIVE, committed-frozen) + versioned
        # immutable type/retention specs; server-verified upload (hash/size mismatch rejected); scan gate;
        # classification downgrade needs platform authority; legal hold blocks disposal; disposition = request ->
        # SoD approval -> execute with tombstone; document ACL supplements (never replaces) RBAC. Storage/scan/
        # extraction/e-sign are Framework-Only ports (deterministic doubles, no cloud/AV/OCR/e-sign, no secrets).
        # Publishes document.lifecycle through the ONE m06 outbox.
        certification_2_5:
          module: m09-docs
          substage: "2.5"
          branch: feature/stage-2-5-m09-documents
          certification_branch: cert/stage-2-5-m09-documents
          parent_baseline: 30b69c2534f7ce224d278c73b3872019f5472ac9   # certified Stage 2.4 main (PR #17 merge)
          # Merged 2026-07-26 via PR #18 (squash) onto certified Stage 2.4 main. Post-merge certification recorded on branch.
          implementation_status: merged
          implementation_pr: 18
          implementation_head: ac3c24b962602b95332ded543fa92a716c919f11
          implementation_merge_sha: 9b99c218e3cc74ca2c2fea45881538dd897a0de3
          merge_status: merged
          ci_implementation: passed                     # PR #18 run 30207851985 on ac3c24b: Smoke + DB (PostgreSQL 16) both green
          smoke_certification: passed_locally           # 15 suites, 2261 assertions, 0 failures
          postgresql_certification: passed_locally      # 22 db specs, 626 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                       # cert PR #19 run 30208538613 + main push 30208900775 (PostgreSQL 16) all green
          certification_status: certified                # cert PR #19 merged 2026-07-26
          certified_baseline_sha: 6aa474426b069182db5037b6c93ba5e305e2feba   # merged main SHA (PR #19) tested for certification
          certification_pr: 19
          certification_merge_sha: 6aa474426b069182db5037b6c93ba5e305e2feba
          local_reverification: {smoke_suites: 15, smoke_assertions: 2261, conformance_assertions: 828,
                                 db_specs: 22, db_assertions: 626, migrations: 18, postgres_version: "15.2",
                                 node_version: "22.14.0", clean_checkout: true, tree_identical_to_ci_head: true}
          adrs: [ADR-044, ADR-045, ADR-046, ADR-047, ADR-048, ADR-049, ADR-050, ADR-051]
          docs: [docs/build/stages/STAGE_2_5_M09_DOCUMENTS_ARCHITECTURE.md,
                 docs/build/stages/STAGE_2_5_M09_DOCUMENTS_READINESS.md,
                 docs/build/stages/STAGE_2_5_M09_DOCUMENTS_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_2_5_M09_DOCUMENTS_COMPLETION.md,
                 docs/build/stages/STAGE_2_5_M09_DOCUMENTS_CERTIFICATION.md]
      - module: m04-admin
        capability: Admin console (tenant/user/role/config)
        status: implemented   # Stage 1 — ORCHESTRATION-ONLY admin console built on branch feature/stage-1-m04-admin-console. Owns 4 FORCE-RLS console tables (NO mirrors), 30 admin.* permissions (17 privileged, 2 platform), 29 ADMIN_ audit codes, NO event family/outbox. Smoke + DB (PostgreSQL 16 CI) to certify post-merge.
        mvp: true
        db_ownership: [admin_saved_view(per-admin filter), admin_preference(per-admin key/value), admin_operation_request(governed
                       admin-operation aggregate; idempotency-keyed; opaque target refs), admin_operation_history(append-only)]
        api_ownership: [/api/v1/admin]
        event_ownership: []   # naming-map authoritative: NO event family (console over m01/m02/m03/m06/m07/m08; reuses their events)
        shared_services_consumed: [m01, m02, m03, m06, m07, m08]
        emits_events: []
        consumes_events: []
        permissions_required: [admin.*]
        audit_prefix: ADMIN_   # 29 codes
        hard_rules: [orchestration_only, no_mirror_tables, delegated_authority, platform_vs_tenant_boundary, default_deny, no_event_family, no_second_outbox_or_engine, redaction_of_secrets_contacts_narratives]
        boundary: "ORCHESTRATION ONLY: a tenant + platform administration surface OVER m01 tenancy, m02 identity/auth/RBAC, m03 audit,
                   m06 workflow, m07 rules, m08 notifications. Owns only its 4 admin_* console tables (saved views, preferences,
                   governed admin-operation ledger) — NO mirror of tenant/identity/account/role/assignment/SoD/audit/workflow/rules/
                   notification data (those exist once, owned by their modules, reached through their PUBLIC services). Every route
                   authenticated + declares a 3-segment admin.* permission (tenant vs platform vs privileged; no vague bypass) +
                   enforces platform-vs-tenant scope; delegated mutations are authorized + audited by the owning module (M04 adds an
                   admin-facing operation trail + audits its own sensitive reads). NO event family, NO second outbox, NO duplicate
                   engine, NO universal admin bypass, NO secret/token/contact exposure. SystemContext is not a universal allow; a
                   tenant admin can never become/act-as platform admin, cross tenants, assign platform roles, or modify immutable
                   system roles (all enforced by the owning modules and honoured unchanged)."
        testing_required: [smoke, db_spec, services_db_spec]
        tests_present: [m04-admin.smoke (37), m04-admin.db-spec (29), m04-services.db-spec (16)]
        divergence_note: "Full HTTP surface: the owned console (operations/saved-views/preferences/dashboard) + tenant administration are
                          wired under /api/v1/admin; the remaining orchestration areas (identity/RBAC/workflow/rules/notifications/audit)
                          ship as tested library services in @finapp/m04-admin following the identical delegated pattern and are exposed
                          incrementally. No module spec pre-existed; see ADR-103."
        adrs: [ADR-103, ADR-104]
        certification_1_m04:
          module: m04-admin
          stage: "1"
          branch: feature/stage-1-m04-admin-console
          certification_branch: cert/stage-1-m04-admin
          parent_baseline: fed1c58e166e3389161134896dcfc3bf25e56f9d   # main after governance PR #52 (m04 approved_for_build)
          implementation_pr: 53
          implementation_head: e77f73db7ead4aa6add3d7bdb216d8c3aa58d40d   # reviewed head
          implementation_merge_sha: e007437e231f0a21de3f1b61c1aa71499148d446   # squash merge onto main (1 parent)
          merge_status: merged
          merged_at: "2026-08-04T18:25:03Z"
          tree_identical_to_reviewed_head: true   # git diff e77f73d e007437 is empty (byte-identical)
          ci_implementation: passed   # run 30938009772 head e77f73d: Smoke + DB (PostgreSQL 16) both success
          ci_main_push: passed        # main push e007437: Smoke + DB (PostgreSQL 16) both success
          smoke_certification: passed_locally         # 28 suites, 5229 assertions, 0 failures (m04 smoke 37, conformance 2903)
          postgresql_certification: passed_locally     # 58 db specs, 1853 assertions, 0 failures (PostgreSQL 15.2 throwaway; m04-admin 29 + m04-services 16)
          lint: passed                                 # 0 errors
          certification_status: certified_on_branch    # cert PR pending merge
          orchestration_only: true
          local_reverification: {tables_force_rls: 4, tenant_isolation_policies: 4, delete_grants: 0,
                                 append_only_update_grants: 0, permissions: 30, privileged: 17, permissions_3seg: 30,
                                 mirror_tables: 0, canonical_tables_once: true, audit_codes: 29, registered_code_count: 672,
                                 operation_idempotency_index: true, outboxes: 1, db_specs: 58, db_assertions: 1853,
                                 smoke_assertions: 5229, migrations: 44, postgres_version: "15.2", clean_checkout: true,
                                 wiped_dist: true}
          verdicts: {orchestration_only: PASS, audit: PASS, no_event_family: PASS, permissions_platform_vs_tenant: PASS,
                     idempotency_concurrency: PASS, privacy_redaction: PASS, contamination: CLEAN}
          documented_limitation: "Only the owned console + tenant-administration HTTP surface is wired; the remaining orchestration areas ship as tested library services and receive controllers incrementally."
          docs: [docs/build/stages/STAGE_1_M04_ADMIN_CONSOLE_COMPLETION.md,
                 docs/build/stages/STAGE_1_M04_ADMIN_CONSOLE_CERTIFICATION.md]
          # certification_pr recorded once the docs-only cert PR is opened.

  - stage: 2
    name: Operational modules
    status: documented
    priority: P0
    mvp: true
    depends_on_stage: 1
    modules:
      - module: m12-feedback
        capability: Feedback Management (enterprise, closed loop)
        status: implemented   # Stage 3.1 — implemented_on_branch (not merged/certified); see certification_3_1
        mvp: true
        db_ownership: [feedback_source_system(configurable), feedback_category(configurable),
                       feedback_questionnaire(versioned spec, one ACTIVE per code, immutable-after-publish),
                       feedback_sla_policy(versioned spec, one ACTIVE per code, immutable-after-publish),
                       feedback_source_transaction(ingestion, external-tx unique + idempotent),
                       feedback_record(core aggregate; customer_contact + narrative SENSITIVE; 15-state lifecycle),
                       feedback_answer(append-only), feedback_queue_item(single-winner claim),
                       feedback_contact_attempt(append-only), feedback_assignment_history(append-only),
                       feedback_activity, feedback_resolution(one per feedback),
                       feedback_sla_instance(one per feedback, deterministic clock), feedback_case_handoff(pending
                       unique + idempotent), feedback_relationship(duplicate/related)]
        api_ownership: [/api/v1/feedback]
        event_ownership: [feedback.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06, m07, m08, m09]
        permissions_required: [feedback.*]
        testing_required: [smoke, db_spec, services_db_spec, api_http_spec]
        tests_present: [m12-feedback.smoke (61), m12-feedback.db-spec (27), m12-services.db-spec (36),
                        api-feedback.db-spec (14)]
        # Enterprise feedback-management platform (not a survey table) for post-transaction customer calls across
        # loans, insurance, bid/performance bonds, advance-payment guarantees and trade finance. Nothing is
        # Aptic-specific: sources, products, departments, branches, officers and transaction types are CONFIGURABLE.
        # Full lifecycle: ingest -> contact queue (single-winner) -> capture (deterministic CSAT/NPS) -> classify ->
        # assign -> resolve (maker-checker SoD) -> rule-gated closure, with reopen / converted_to_case branches.
        # Reuses m06 workflow, m07 rules, m08 escalation+notifications, m09 documents THROUGH EVENTS/CONTRACTS only
        # (no duplicate engines). SLA is deterministic via a Clock port. Case handoff to m13 is a port + pending
        # record + versioned event ONLY (no case table, no second escalation engine). Customer contacts + narratives
        # are sensitive: RLS-stored, redacted on read (feedback.customer_contact.read), NEVER in events/audit.
        # Publishes feedback.lifecycle (24 event types) through the ONE m06 outbox.
        certification_3_1:
          module: m12-feedback
          substage: "3.1"
          branch: feature/stage-3-1-m12-feedback-management
          certification_branch: cert/stage-3-1-m12-feedback-management
          parent_baseline: 6aa474426b069182db5037b6c93ba5e305e2feba   # certified Stage 2.5 main (PR #19 merge)
          # Merged 2026-07-26 via PR #20 (squash) onto certified Stage 2.5 main; certified via cert PR #21 (squash) → main 936e337.
          implementation_status: merged
          implementation_pr: 20
          implementation_head: de604d5b1fb7f5281a3048fd19a94f8fb3d40433
          implementation_merge_sha: f9f32dfb27a524ff9eb5385e2576866143d2236c
          merge_status: merged
          ci_implementation: passed                      # PR #20 head de604d5: Smoke + DB (PostgreSQL 16) both green
          smoke_certification: passed_locally            # 16 suites, 2476 assertions, 0 failures (incl. m12 smoke 61 + conformance 976)
          postgresql_certification: passed_locally       # 25 db specs, 703 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                        # cert PR #21 head 9dbdad2 + main push 936e337: Smoke + DB (PostgreSQL 16) both green
          certification_status: certified                 # cert PR #21 merged 2026-07-26
          certification_pr: 21
          certification_merge_sha: 936e33779e0944b0a1630fd87220a2c1447fe2d9
          certified_baseline_sha: 936e33779e0944b0a1630fd87220a2c1447fe2d9   # merged main SHA (cert PR #21) — certified Stage 3.1 baseline
          tree_identical_to_reviewed_head: true          # git diff de604d5 f9f32df is empty (byte-identical)
          local_reverification: {smoke_suites: 16, smoke_assertions: 2476, conformance_assertions: 976,
                                 db_specs: 25, db_assertions: 703, migrations: 20, postgres_version: "15.2",
                                 node_version: "22.14.0", clean_checkout: true, tree_identical_to_ci_head: true}
          adrs: [ADR-052, ADR-053, ADR-054, ADR-055, ADR-056]
          docs: [docs/build/stages/STAGE_3_1_M12_FEEDBACK_ARCHITECTURE.md,
                 docs/build/stages/STAGE_3_1_M12_FEEDBACK_READINESS.md,
                 docs/build/stages/STAGE_3_1_M12_FEEDBACK_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_3_1_M12_FEEDBACK_COMPLETION.md,
                 docs/build/stages/STAGE_3_1_M12_FEEDBACK_CERTIFICATION.md]
          # certification_pr recorded once the docs-only cert PR is opened.
      - module: m13-case
        capability: Case Management (enterprise; cases/complaints/investigation/legal/recovery)
        status: implemented   # Stage 3.2 — implemented_on_branch (not merged/certified); see certification_3_2
        mvp: true
        db_ownership: [case_type(versioned spec, one ACTIVE), case_sla_policy(versioned spec, one ACTIVE),
                       case_record(core aggregate; SENSITIVE fields + inline legal/recovery dims; 18-state lifecycle),
                       case_handoff_intake(M12 idempotency ledger), case_status_history(append-only),
                       case_assignment_history(append-only), case_party, case_activity(also correspondence),
                       case_task, case_issue, case_investigation, case_finding(append-only), case_document(m09 refs),
                       case_evidence, case_deadline, case_hearing, case_decision(maker-checker + SoD CHECK),
                       case_settlement, case_note(append-only; confidential/privileged), case_relationship]
        api_ownership: [/api/v1/cases]
        event_ownership: [case.lifecycle, case.converted_to_matter]
        shared_services_consumed: [kernel, m01, m02, m03, m06, m07, m08, m09, m12]
        consumes_events: [feedback.lifecycle (CaseHandoffRequested / feedback.escalated)]
        emits_events: [case.converted_to_matter (to m14)]
        permissions_required: [cases.*]
        testing_required: [smoke, db_spec, services_db_spec, api_http_spec]
        tests_present: [m13-case.smoke (67), m13-case.db-spec (34), m13-services.db-spec (31),
                        api-cases.db-spec (15)]
        # Enterprise, configurable, multi-tenant case-management platform (not merely a case table): cases,
        # complaints, investigations, legal-matter support, litigation + recovery/enforcement tracking, internal/
        # regulatory/incident matters. NOTHING Aptic-/Kenya-specific: case types, jurisdictions, references, SLA
        # policies are CONFIGURABLE. Full lifecycle: intake (manual / external / idempotent M12 handoff) -> triage
        # -> assignment -> investigation/legal/recovery -> maker-checker decision -> resolution -> rule-gated
        # closure, with reopen / archived. Reuses m06 workflow, m07 rules, m08 escalation+notifications, m09
        # documents/evidence and the m12 handoff THROUGH EVENTS/CONTRACTS + PORTS only (no duplicate engines). SLA
        # + deadlines are deterministic via a Clock port. Decision + settlement approval are maker-checker
        # (submitter != approver). Recovery/settlement store finance REFERENCES only (no ledger/posting/payment).
        # Party contacts, privileged notes, correspondence bodies + confidential settlement terms are sensitive:
        # RLS-stored, redacted on read, NEVER in events/audit. M12 handoff = one case per handoff (idempotent);
        # M14 boundary = case.converted_to_matter event (m13 owns no legal-matter tables). Publishes
        # case.lifecycle (32 event types) + case.converted_to_matter (1) through the ONE m06 outbox.
        certification_3_2:
          module: m13-case
          substage: "3.2"
          branch: feature/stage-3-2-m13-case-management
          certification_branch: cert/stage-3-2-m13-case-management
          parent_baseline: 936e33779e0944b0a1630fd87220a2c1447fe2d9   # certified Stage 3.1 main (cert PR #21 merge)
          # Merged 2026-07-26 via PR #22 (squash) onto certified Stage 3.1 main; certified via cert PR #23 (squash) → main 1262845.
          implementation_status: merged
          implementation_pr: 22
          implementation_head: 5cf058637a3dd03fd08ee0a5928a5e19c977b679
          implementation_merge_sha: 4c5ae6a41d2d693014ecb6a603f967e542c6c28f
          merge_status: merged
          ci_implementation: passed                      # PR #22 head 5cf0586 + main push 4c5ae6a: Smoke + DB (PostgreSQL 16) both green
          smoke_certification: passed_locally            # 17 suites, 2771 assertions, 0 failures (incl. m13 smoke 67 + conformance 1193)
          postgresql_certification: passed_locally       # 28 db specs, 783 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                        # cert PR #23 head 52d1eae + main push 1262845: Smoke + DB (PostgreSQL 16) both green
          certification_status: certified                 # cert PR #23 merged 2026-07-27
          certification_pr: 23
          certification_merge_sha: 12628451a9b4427724d08adbca8b9d626160aa92
          certified_baseline_sha: 12628451a9b4427724d08adbca8b9d626160aa92   # merged main SHA (cert PR #23) — certified Stage 3.2 baseline
          tree_identical_to_reviewed_head: true          # git diff 5cf0586 4c5ae6a is empty (byte-identical)
          local_reverification: {smoke_suites: 17, smoke_assertions: 2771, conformance_assertions: 1193,
                                 db_specs: 28, db_assertions: 783, migrations: 22, postgres_version: "15.2",
                                 node_version: "22.14.0", clean_checkout: true, tree_identical_to_ci_head: true}
          adrs: [ADR-057, ADR-058, ADR-059, ADR-060]
          docs: [docs/build/stages/STAGE_3_2_M13_CASES_ARCHITECTURE.md,
                 docs/build/stages/STAGE_3_2_M13_CASES_READINESS.md,
                 docs/build/stages/STAGE_3_2_M13_CASES_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_3_2_M13_CASES_COMPLETION.md,
                 docs/build/stages/STAGE_3_2_M13_CASES_CERTIFICATION.md]
          # certification_pr recorded once the docs-only cert PR is opened.

  - stage: 3
    name: Finance
    status: documented
    priority: P0
    mvp: true
    mvp_note: Reconciliation + draft journals in MVP; posting to core systems is DEFERRED.
    depends_on_stage: 1
    modules:
      - module: m19-finance
        capability: Finance Operations Foundation (the finance reference-data root — chart of accounts, fiscal calendar, currencies/FX, dimensions, tax, config)
        status: implemented   # Stage 3.1 — implemented_on_branch (not merged/certified); see certification_3_1
        mvp: true
        db_ownership: [finance_entity(accounting entity; hierarchy), finance_account_type(class + normal side),
                       finance_account(chart of accounts; tree; postable; lifecycle; SoD-free config), finance_account_history(append-only),
                       finance_fiscal_year(open/closed), finance_fiscal_period(open/closed/locked; the posting gate),
                       finance_period_history(append-only), finance_currency(ISO code + minor units),
                       finance_exchange_rate(EXACT-decimal FX; idempotent natural key), finance_entity_currency(functional/presentation/transaction),
                       finance_cost_center(tree), finance_dimension, finance_dimension_value,
                       finance_tax_code, finance_tax_rate(EXACT-decimal, effective-dated), finance_payment_term,
                       finance_config(versioned, immutable-after-publish, one-active, idempotency-keyed), finance_config_history(append-only)]
        api_ownership: [/api/v1/finance]
        event_ownership: [finance.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06]
        emits_events: [finance.lifecycle (33 event types)]
        permissions_required: [finance.*]
        hard_rules: [decimal_safe, no_float, no_monetary_amounts_in_foundation, no_posting, no_journals, tenant_isolation]
        testing_required: [smoke, db_spec, services_db_spec, api_http_spec]
        tests_present: [m19-finance.smoke (73), m19-finance.db-spec (37), m19-services.db-spec (60),
                        api-finance.db-spec (12)]
        # The finance FOUNDATION / reference-data root of the finance domain, consumed by reconciliation (m15),
        # GL reconciliation (m20) and the journal engine (m21). Owns accounting entities, the chart of accounts
        # (account types + ledger accounts), the fiscal calendar (fiscal years + accounting periods with an
        # open/closed/locked state), currencies + EXACT-decimal FX rates, cost centres, analytical dimensions, tax
        # codes + EXACT-decimal effective-dated rates, payment terms, and versioned finance configuration. Owns NO
        # journals, NO journal lines, NO postings/ledger-posting engine (m21), NO reconciliation (m15/m15a), NO GL
        # reconciliation (m20), NO approval workflow (m22), NO finance integration (m23), NO AI (m27), NO cash/AR/AP/
        # payments. Carries NO monetary amounts or balances — the only money-adjacent values are EXACT NUMERIC FX +
        # tax rates, never float (ADR-007/078). Period close/lock is the cross-module "no posting into a closed
        # period" gate downstream posting (m21) reads (ADR-078). 18 FORCE-RLS tables, composite keys, 3 append-only
        # ledgers, no DELETE grant; ledger accounts/periods/config have explicit state machines; config is
        # immutable-after-publish (one active per entity+scope) + idempotency-keyed; optimistic-concurrency CAS.
        # Reuses m02 (authz), m03 (audit), m06 (workflow + the ONE outbox) THROUGH tokens/events only. GAPs closed at
        # 3.1: the 18-table composition (ADR-077; repo gave only the count), /api/v1/finance (GAP-2), finance.*
        # namespace (GAP-4), the finance.lifecycle event family (ADR-079; naming-map had none). 45 permissions (16
        # privileged), 34 FIN_ audit codes (prefix shared with m23), finance.lifecycle (33 event types).
        certification_3_1:
          module: m19-finance
          substage: "3.1"
          branch: feature/stage-3-1-m19-finance-operations
          certification_branch: cert/stage-3-1-m19-finance-operations
          parent_baseline: 470d19a5b8aba933270ca360a271437c93afbe34   # governance-approved main (PR #34 merge)
          governance_approval_pr: 34                     # m19-finance documented -> approved_for_build (PR #34, main 470d19a)
          # Merged 2026-07-29 via PR #35 (squash) onto governance-approved main 470d19a; post-merge certification on branch.
          implementation_status: merged
          implementation_pr: 35
          implementation_head: 712e0a17c3ee8ce1acd07944a54ab9197bdfbda5
          implementation_merge_sha: b0f86f84bb57aac06ff2d0b9d018a451b5f2da61
          merge_status: merged
          ci_implementation: passed                      # PR #35 head 712e0a1 + merge b0f86f8: Smoke lane + DB lane (PostgreSQL 16) both green
          smoke_certification: passed_locally            # 22 suites, 4238 assertions, 0 failures (incl. m19 smoke 73 + conformance 2280)
          postgresql_certification: passed_locally       # 43 db specs, 1410 assertions, 0 failures; fresh replay of 32 migrations (PostgreSQL 15.2 throwaway)
          ci_certification: pending                       # cert PR CI recorded once green (see certification report §S)
          certification_status: certified_on_branch      # cert PR pending, not merged (stopped before merge)
          certified_baseline_sha: b0f86f84bb57aac06ff2d0b9d018a451b5f2da61   # merged main SHA (PR #35) tested for certification
          tree_identical_to_reviewed_head: true          # git diff 712e0a1 b0f86f8 is empty (byte-identical)
          local_reverification: {smoke_suites: 22, smoke_assertions: 4238, conformance_assertions: 2280,
                                 db_specs: 43, db_assertions: 1410, migrations_replayed: 32, postgres_version: "15.2",
                                 clean_checkout: true, tree_identical_to_ci_head: true,
                                 m19_tables: 18, force_rls: 18, composite_pk: 18, delete_grants: 0,
                                 append_only_ledgers: 3, composite_fks: 23, single_col_fks: 0, float_columns: 0,
                                 numeric_rate_columns: 2, permissions: 45, privileged_permissions: 16,
                                 audit_codes: 34, registered_code_count: 520, event_types: 33, event_families: 16,
                                 mutating_routes: 38, read_routes: 21, outbox_tables: 1}
          certification_pr: pending
          certification_merge_sha: pending
          adrs: [ADR-077, ADR-078, ADR-079, ADR-080]
          docs: [docs/build/stages/STAGE_3_1_M19_FINANCE_ARCHITECTURE.md,
                 docs/build/stages/STAGE_3_1_M19_FINANCE_READINESS.md,
                 docs/build/stages/STAGE_3_1_M19_FINANCE_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_3_1_M19_FINANCE_COMPLETION.md,
                 docs/build/stages/STAGE_3_1_M19_FINANCE_CERTIFICATION.md]
      - module: m15-recon + m15a-matching
        capability: Bank Reconciliation + Matching Engine (deterministic, explainable)
        status: implemented   # Stage 3 — implemented_on_branch (not merged/certified); see certification_3
        mvp: true
        db_ownership: [recon_bank_account, recon_matching_ruleset(versioned, one-active), recon_matching_rule,
                       recon_ruleset_history(append-only), recon_statement_import(dup-protected by file hash),
                       recon_statement_line(bigint minor units), recon_ledger_import, recon_ledger_entry(bigint minor units),
                       recon_run(lifecycle; the reconciliation), recon_status_history(append-only),
                       recon_match(1:1/1:many/many:1/split/grouped; idempotency-keyed), recon_match_line(append-only),
                       recon_match_candidate(append-only engine evidence; score/band/variances/reason codes),
                       recon_exception(aging; required-exception gate), recon_manual_decision(append-only override evidence),
                       recon_run_summary(append-only), recon_note(append-only), recon_import_error(append-only)]
        # m15a-matching owns ZERO tables — a PURE deterministic explainable engine (scoring/confidence/colour/
        # match-type/split-balance) consumed by m15-recon.
        api_ownership: [/api/v1/reconciliation]
        event_ownership: [reconciliation.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06, m07, m09, m19, m15a]
        consumes_events: [none inbound; m27 finance-AI suggestions are OPTIONAL (Stage 5, not required for MVP)]
        emits_events: [reconciliation.lifecycle (24 event types)]
        permissions_required: [reconciliation.*]
        hard_rules: [decimal_safe, integer_minor_units, no_float, deterministic_matching, manual_override_append_only,
                     no_autoclose_with_open_required_exception, tenant_isolation]
        testing_required: [smoke, db_spec, services_db_spec, matching_engine_spec, api_http_spec]
        tests_present: [m15a-matching.smoke (43), m15-recon.db-spec (36), m15-services.db-spec (37),
                        api-reconciliation.db-spec (11)]
        # Bank reconciliation + the matching engine: multi-bank accounts; statement + ledger ingestion (CSV/Excel/PDF/
        # API, DUPLICATE-PROTECTED by per-account file hash) with normalized lines in INTEGER MINOR UNITS; versioned
        # matching rulesets (immutable-after-publish, one-active); reconciliation runs with a controlled lifecycle
        # (draft -> matching -> review -> completed, reopened); the DETERMINISTIC, EXPLAINABLE matching engine (m15a,
        # PURE, zero tables) producing candidates with score/confidence band/colour status/variances(minor units)/
        # reason codes; exact/probable/split/grouped matches (1:1/1:many/many:1); exception management + aging with a
        # required-exception completion gate; manual review/override as APPEND-ONLY evidence that never overwrites the
        # engine's candidate evidence; run summaries + notes. Money is INTEGER MINOR UNITS (bigint) — never float
        # (ADR-007/082). Matching is reproducible from the same inputs + ruleset version; AI suggestions (m27) are an
        # OPTIONAL separable layer — reconciliation works fully without them (ADR-082). 18 FORCE-RLS tables, composite
        # keys, 8 append-only ledgers, no DELETE grant. Reuses m02 (authz), m03 (audit), m06 (workflow + the ONE
        # outbox), m07 (rules), m09 (documents, opaque ref), m19 (finance foundation, opaque entity/currency ref).
        # Owns NO chart of accounts (m19), NO GL reconciliation (m20), NO journals/postings (m21), NO approvals (m22),
        # NO integration posting (m23), NO AI models (m27). 29 permissions (11 privileged), 25 RECON_ audit codes,
        # reconciliation.lifecycle (24 event types). ADR-081..084.
        certification_3:
          module: m15-recon + m15a-matching
          substage: "3"
          branch: feature/stage-3-m15-reconciliation-matching
          certification_branch: cert/stage-3-m15-reconciliation-matching
          parent_baseline: f01f9cff73838c9347a90de9f95e71c8c739ec51   # governance-approved main (PR #37 merge)
          governance_approval_pr: 37                     # documented -> approved_for_build (PR #37, main f01f9cf)
          # Merged 2026-07-30 via PR #38 (squash) onto governance-approved main f01f9cf; post-merge certification on branch.
          implementation_status: merged
          implementation_pr: 38
          implementation_head: 640c008f42249aa436b110b0825ab592e14a3db0
          implementation_merge_sha: 46880e71e8365e99c55b101e6a67b9793d460c14
          merge_status: merged
          ci_implementation: passed                      # PR #38 head 640c008 + merge 46880e7: Smoke lane + DB lane (PostgreSQL 16) both green
          smoke_certification: passed_locally            # 23 suites, 4407 assertions, 0 failures (incl. m15a-matching 43 + conformance 2400)
          postgresql_certification: passed_locally       # 46 db specs, 1494 assertions, 0 failures; fresh replay of 34 migrations (PostgreSQL 15.2 throwaway)
          ci_certification: pending                       # cert PR CI recorded once green (see certification report)
          certification_status: certified_on_branch      # cert PR pending, not merged (stopped before merge)
          certified_baseline_sha: 46880e71e8365e99c55b101e6a67b9793d460c14   # merged main SHA (PR #38) tested for certification
          tree_identical_to_reviewed_head: true          # git diff 640c008 46880e7 is empty (byte-identical)
          local_reverification: {clean_checkout: true, tree_identical_to_ci_head: true, matching_deterministic: true,
                                 smoke_suites: 23, smoke_assertions: 4407, conformance_assertions: 2400,
                                 db_specs: 46, db_assertions: 1494, migrations_replayed: 34, postgres_version: "15.2",
                                 m15_tables: 18, m15a_tables: 0, force_rls: 18, policies: 18, delete_grants: 0,
                                 append_only_ledgers: 8, composite_fks: 19, single_col_fks: 0, float_columns: 0,
                                 bigint_money_columns: 10, permissions: 29, privileged_permissions: 11,
                                 audit_codes: 28, registered_code_count: 548, event_types: 24, event_families: 17,
                                 mutating_routes: 20, read_routes: 22, outbox_tables: 1}
          certification_pr: 39
          certification_head: 1fe90a18363bf44aedb66a14077c932f015f7606
          certification_merge_sha: pending
          adrs: [ADR-081, ADR-082, ADR-083, ADR-084]
          docs: [docs/build/stages/STAGE_3_M15_RECONCILIATION_ARCHITECTURE.md,
                 docs/build/stages/STAGE_3_M15_RECONCILIATION_READINESS.md,
                 docs/build/stages/STAGE_3_M15_RECONCILIATION_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_3_M15_RECONCILIATION_COMPLETION.md,
                 docs/build/stages/STAGE_3_M15_RECON_CERTIFICATION.md]
      - module: m20-glrecon
        capability: GL reconciliation
        status: implemented   # Stage 3 — implemented_on_branch (not merged/certified); see certification_4
        mvp: true
        db_ownership: [gl_recon_account, gl_ruleset(versioned, one-active), gl_rule, gl_ruleset_history(append-only),
                       gl_import(dup-protected by file hash), gl_import_error(append-only),
                       gl_balance(invariant-checked; opening/debits/credits/closing), gl_line(bigint minor units),
                       gl_source_import, gl_source_line(bigint minor units), gl_recon_run(lifecycle),
                       gl_run_status_history(append-only), gl_run_balance(append-only; invariant evidence),
                       gl_match(1:1/1:many/many:1/split/grouped; idempotency-keyed), gl_match_line(append-only),
                       gl_match_candidate(append-only engine evidence; score/band/variances/reason codes),
                       gl_reconciling_item(aging), gl_exception(open/under_review/resolved/waived; required-exception gate),
                       gl_manual_decision(append-only override evidence), gl_certification(privileged override gate),
                       gl_certification_history(append-only), gl_journal_recommendation(DRAFT-only; is_draft CHECK),
                       gl_run_summary(append-only), gl_note(append-only)]
        api_ownership: [/api/v1/gl-reconciliation]
        event_ownership: [glrecon.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06, m15a, m19]
        consumes_events: [none inbound; m27 finance-AI suggestions are OPTIONAL (Stage 5, not required for MVP)]
        emits_events: [glrecon.lifecycle (33 event types)]
        downstream_consumers: [m21 (draft journal recommendations handed off; never posted by m20), m22 (approval)]
        permissions_required: [gl_reconciliation.*]
        hard_rules: [decimal_safe, integer_minor_units, no_float, gl_balance_invariant, deterministic_matching,
                     reuse_m15a_no_duplicate_matching, manual_override_append_only,
                     no_autoclose_with_open_required_exception, certification_override_privileged_with_reason,
                     draft_only_recommendations_never_post, tenant_isolation]
        testing_required: [smoke, db_spec, services_db_spec, matching_engine_spec, api_http_spec]
        tests_present: [m20-glrecon.smoke (54), m20-glrecon.db-spec (48), m20-services.db-spec (31),
                        api-gl-reconciliation.db-spec (14)]
        # GL reconciliation: reconcile GL balances + transactions against a source system; the balance invariant
        # (opening + debits - credits = calculated closing) in INTEGER MINOR UNITS, enforced in the engine AND by DB
        # CHECKs on gl_balance + gl_run_balance; multi-account GL/source ingestion (DUPLICATE-PROTECTED by per-account
        # file hash); versioned matching rulesets (immutable-after-publish, one-active); reconciliation runs with a
        # controlled lifecycle (draft -> running -> review_required -> completed/failed, reopened); DETERMINISTIC,
        # EXPLAINABLE matching that REUSES the PURE m15a engine (no duplicated matching logic) producing candidates
        # with score/band/colour/variances(minor units)/reason codes; reconciling items + exceptions (aging) with a
        # required-exception completion gate; manual review/override as APPEND-ONLY evidence that never overwrites the
        # engine's candidate evidence; balance certification that fails closed over open blockers unless a PRIVILEGED
        # override carrying a reason is supplied (permission + non-empty reason + DB CHECK); and DRAFT journal
        # recommendations (is_draft always true, DB-CHECKed) handed off to m21 -- m20 NEVER posts a journal, writes to
        # the general ledger, or approves anything. Money is INTEGER MINOR UNITS (bigint) -- never float (ADR-007/088).
        # 24 FORCE-RLS tables, composite keys, 10 append-only ledgers, no DELETE grant. Reuses m02 (authz), m03
        # (audit), m06 (workflow + the ONE outbox), m15a (matching engine), m19 (finance foundation, opaque
        # account/currency ref). Owns NO chart of accounts (m19), NO bank reconciliation (m15), NO journals/postings
        # (m21), NO approvals (m22), NO integration posting (m23), NO AI models (m27). 35 permissions (11 privileged),
        # 37 GLRECON_ audit codes, glrecon.lifecycle (33 event types). ADR-085..090.
        certification_4:
          module: m20-glrecon
          substage: "3"
          branch: feature/stage-3-m20-gl-reconciliation
          certification_branch: cert/stage-3-m20-gl-reconciliation
          parent_baseline: 6cf749bf4c798e4863d564693e04dfcdcacce2d5   # governance-approved main (PR #40 merge)
          governance_approval_pr: 40                     # documented -> approved_for_build (PR #40, main 6cf749b)
          # Merged 2026-07-31 via PR #41 (squash) onto governance-approved main 6cf749b; post-merge certification on branch.
          implementation_status: merged
          implementation_pr: 41
          implementation_head: 862d6e3caecaf7b4d881bbf4712e3035968b4bad
          implementation_merge_sha: c05a7722a9262cbf111320a7be31802e05c43204
          merge_status: merged
          ci_implementation: passed                      # PR #41 head 862d6e3 + merge c05a772: Smoke lane + DB lane (PostgreSQL 16) both green
          smoke_certification: passed_locally            # 24 suites, 4619 assertions, 0 failures (incl. m20-glrecon 54 + conformance 2551)
          postgresql_certification: passed_locally       # 49 db specs, 1587 assertions, 0 failures; fresh replay of 36 migrations (PostgreSQL 15.2 throwaway)
          ci_certification: passed                        # cert PR #42 head 5933f77: Smoke lane + DB lane (PostgreSQL 16) both green
          certification_status: certified_on_branch      # cert PR pending, not merged (stopped before merge)
          certified_baseline_sha: c05a7722a9262cbf111320a7be31802e05c43204   # merged main SHA (PR #41) tested for certification
          tree_identical_to_reviewed_head: true          # git diff 862d6e3 c05a772 is empty (byte-identical)
          local_reverification: {clean_checkout: true, tree_identical_to_ci_head: true, matching_deterministic: true,
                               balance_invariant_deterministic: true,
                               smoke_suites: 24, smoke_assertions: 4619, conformance_assertions: 2551,
                               db_specs: 49, db_assertions: 1587, migrations_replayed: 36, postgres_version: "15.2",
                               m20_tables: 24, force_rls: 24, policies: 24, delete_grants: 0, append_only_ledgers: 10,
                               composite_fks: 27, single_col_fks: 0, float_columns: 0, bigint_money_columns: 26,
                               balance_invariant_checks: 2, draft_only_checks: 1, override_checks: 1,
                               permissions: 35, privileged_permissions: 11, audit_codes: 37, registered_code_count: 585,
                               event_types: 33, event_families: 18, mutating_routes: 30, read_routes: 31, outbox_tables: 1}
          certification_pr: 42
          certification_head: 13630117975699d9f2119082eac86233082918cc
          certification_merge_sha: pending
          adrs: [ADR-085, ADR-086, ADR-087, ADR-088, ADR-089, ADR-090]
          docs: [docs/build/stages/STAGE_3_M20_GLRECON_ARCHITECTURE.md,
                 docs/build/stages/STAGE_3_M20_GLRECON_COMPLETION.md,
                 docs/build/stages/STAGE_3_M20_GL_RECON_CERTIFICATION.md]
      - module: m21-journal
        capability: Journal engine (DRAFT ONLY in MVP)
        status: implemented   # Stage 3 — implemented_on_branch feature/stage-3-m21-journal (not merged/certified); see certification_3_m21
        mvp: true
        db_ownership: [journal_type(versioned, one-active), journal_config(versioned, immutable-after-publish, one-active, idempotency-keyed),
                       journal_reason_code(configurable reason-code registry), journal_recommendation(m20 handoff intake; opaque refs),
                       journal_recommendation_line(append-only), journal_recommendation_history(append-only),
                       journal_draft(header; balanced-before-advance CHECK; bigint minor-unit totals; lifecycle),
                       journal_line(bigint minor units; direction debit/credit; active/removed), journal_status_history(append-only),
                       journal_draft_balance(append-only; balanced=(debits=credits) CHECK; invariant evidence),
                       journal_note(append-only), journal_validation(append-only; deterministic validation run),
                       journal_validation_finding(append-only; machine-readable reason codes),
                       posting_request(approval-gated; approval_ref + approved_by SoD CHECK; period-open CHECK; idempotency-keyed; DRAFT-first, posting deferred),
                       posting_request_history(append-only), posting_result(evidence; pending/succeeded/failed; idempotency-keyed),
                       posting_result_history(append-only), posting_idempotency(append-only; no-duplicate-post ledger)]
        api_ownership: [/api/v1/journals]
        event_ownership: [journal.lifecycle, posting_request.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06, m07, m19, m20]
        consumes_events: [glrecon.lifecycle RecommendationHandedOff (from m20; fire-and-forget, idempotent intake); m33 posting-push is POST-MVP]
        emits_events: [journal.lifecycle (16 event types), posting_request.lifecycle (6 event types; posting deferred)]
        permissions_required: [journals.*]
        testing_required: [smoke, db_spec_balanced_no_autopost]
        tests_present: [m21-journal.smoke (80), m21-journal.db-spec (44), m21-services.db-spec (28), api-journals.db-spec (14)]
        hard_rules: [decimal_safe, balanced_before_post, no_autopost, no_closed_period_post, no_duplicate_post, maker_not_checker]
        # DRAFT-first journal engine: ingest m20 DRAFT recommendations (opaque refs; RecommendationHandedOff), create/edit BALANCED
        # draft journals (debits==credits in INTEGER MINOR UNITS, DB-enforced balanced-before-advance), deterministic + explainable
        # validation (reason codes), draft lifecycle up to submit-for-approval, and PREPARE approval-gated posting requests + record
        # posting-result evidence. It NEVER approves (m22 owns maker-checker + SoD), NEVER posts to a ledger/ERP/core system, NEVER
        # auto-posts, NEVER posts into a closed/locked period (reads m19 period state), NEVER duplicate-posts (idempotency ledger).
        # 18 FORCE-RLS tables, composite keys, 10 append-only ledgers, no DELETE grant. Reuses m02 (authz), m03 (audit), m06 (workflow
        # + the ONE outbox), m07 (rules), m19 (finance foundation, opaque account/period/currency ref), m20 (recommendation handoff).
        # Owns NO chart of accounts/periods (m19), NO GL reconciliation (m20), NO approval workflow (m22), NO integration/posting push
        # (m23/m33), NO AI (m27). 27 permissions (9 privileged), 23 JOURNAL_ audit codes, journal.lifecycle (16) +
        # posting_request.lifecycle (6, deferred). ADR-091..096.
        certification_3_m21:
          module: m21-journal
          substage: "3"
          branch: feature/stage-3-m21-journal
          certification_branch: cert/stage-3-m21-journal
          governance_pr: 43   # m21 documented -> approved_for_build; governance merge 88b0549
          implementation_pr: 44   # merged 2026-08-03T11:35:20Z
          reviewed_implementation_head: 1d276ebaea06eef70739d2e67a269c62db183506
          implementation_merge_sha: fb743563b1815e1d6d55ece9b815990498bff1f8   # squash on 88b0549; current origin/main
          tree_equivalence: empty_diff   # git diff 1d276eb fb74356 == EMPTY (reviewed tree == merged tree)
          parent_baseline: fb743563b1815e1d6d55ece9b815990498bff1f8   # certification baseline = implementation merge SHA
          implementation_ci: both_pg16_lanes_success   # smoke + PostgreSQL 16 DB, head 1d276eb
          local_gates: format+lint(0err)+build+smoke(25/4816,conformance 2656)+migrate(38 fresh-replay)+db(52/1673 - m21-journal 44, m21-services 28, api-journals 14); PostgreSQL 15.2 (CI is PG16)
          metrics: 18 tables (18 FORCE-RLS, 18 policies, 16 composite FKs, 0 single-col tenant FKs, 43 CHECKs, 0 float, 11 bigint minor, 0 DELETE grants, 10 append-only ledgers), 27 permissions (9 privileged), 23 JOURNAL_ audit codes (registered total 608), journal.lifecycle (16) + posting_request.lifecycle (6, deferred), 20 event families, 22 mutating + 14 read routes, 5 lifecycle machines, 13 reason codes
          status: certified_on_branch   # CERTIFIED ON BRANCH; certification PR open, NOT merged
          report: docs/build/stages/STAGE_3_M21_JOURNAL_CERTIFICATION.md
      - module: m22-approval
        capability: Finance approval workflow (maker-checker + SoD)
        status: implemented   # Stage 3 — built on branch governance/approve-m22-approval; 24 FORCE-RLS tables, 25 permissions (12 privileged), 23 APPROVAL_ audit codes, approval.lifecycle (16 event types). Smoke + DB (PostgreSQL 16 CI) to certify post-merge.
        mvp: true
        db_ownership: [approval_policy(versioned spec, one active per subject_type+scope), approval_policy_step(immutable spec),
                       approval_policy_history(append-only), approval_config(versioned, immutable-after-publish, idempotency-keyed),
                       approval_reason_code(configurable registry), approval_request(core aggregate; the maker-checker + SoD choke point;
                       final_approver != requested_by CHECK; no-approval-without-quorum CHECK), approval_request_step(per-request step),
                       approval_decision(append-only; maker != checker CHECK), approval_status_history(append-only),
                       approval_step_history(append-only), approval_assignment(append-only), approval_delegation(delegate != delegator CHECK),
                       approval_delegation_history(append-only), approval_sod_check(append-only SoD evidence), approval_participant(append-only
                       distinct participant ledger), approval_escalation(append-only; single-fire + depth-bounded), approval_timer(append-only
                       m06 SLA-timer link), approval_notification(append-only m08 dispatch), approval_workflow_link(append-only m06 instance
                       link), approval_idempotency(append-only; unique per key), approval_note(append-only), approval_evidence(append-only m09
                       refs), approval_outcome(append-only terminal + released approval ref), approval_override(append-only; SoD still applies)]
        api_ownership: [/api/v1/approvals]
        event_ownership: [approval.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06, m08]
        emits_events: [approval.lifecycle (16 event types)]
        consumes_events: []
        permissions_required: [approvals.*]
        hard_rules: [maker_checker, segregation_of_duties, terminal_state_protection, optimistic_concurrency, idempotency, no_second_workflow_timer_notification_engine]
        audit_prefix: APPROVAL_   # 23 codes (incl. APPROVAL_SOD_BLOCKED — a refused controlled action is never silent)
        boundary: "Owns the approval workflow ONLY. NEVER approves on behalf of a human (a human decides; m22 records + enforces), NEVER posts
                   to a ledger/ERP (m21/m23 do, gated on the approval reference m22 releases), NEVER stands up a second workflow/timer/
                   notification engine — reuses m06 (workflow + SLA + timers + the ONE outbox) and m08 (notifications) through OPAQUE refs.
                   Owns no journals (m21), chart of accounts/periods (m19) or integration/posting (m23); subject/workflow/timer/notification/
                   document refs are OPAQUE ids (no FK). m23 is downstream (consumes m22), not a prerequisite."
        testing_required: [smoke, db_spec_sod, services_db_spec]
        tests_present: [m22-approval.smoke (72), m22-approval.db-spec (50), m22-services.db-spec (33)]
        certification_3:
          module: m22-approval
          stage: "3"
          branch: feature/stage-3-m22-approval-workflow
          certification_branch: cert/stage-3-m22-approval
          parent_baseline: 1b4ced19b1dbd0d6a400fe711ddefd3688655500   # main after governance PR #46 (m22 approved_for_build)
          implementation_pr: 47
          implementation_head: dba234acd1f98ec371347cf1ba037403d627da39   # reviewed head
          implementation_merge_sha: ec931e79b37d3b1d109e39d1dab52a284f83ef61   # squash merge onto main (1 parent)
          merge_status: merged
          merged_at: "2026-08-04T09:50:19Z"
          tree_identical_to_reviewed_head: true   # git diff dba234a ec931e7 is empty (byte-identical)
          ci_implementation: passed   # run 30895571158 head dba234a: Smoke + DB (PostgreSQL 16) both success
          ci_main_push: passed        # main push ec931e7: Smoke + DB (PostgreSQL 16) both success
          smoke_certification: passed_locally         # 26 suites, 4995 assertions, 0 failures (incl. m22 smoke 72, conformance 2757)
          postgresql_certification: passed_locally     # 54 db specs, 1756 assertions, 0 failures (PostgreSQL 15.2 throwaway; m22-approval 50 + m22-services 33)
          lint: passed                                 # 0 errors (67 pre-existing baseline warnings; m22 adds none)
          certification_status: certified_on_branch    # cert PR pending merge
          local_reverification: {tables_force_rls: 24, tenant_isolation_policies: 24, delete_grants: 0,
                                 append_only_update_grants: 0, float_columns: 0, money_bigint_minor: 2,
                                 permissions: 25, privileged: 12, outboxes: 1, sod_check_constraints: 8,
                                 db_specs: 54, db_assertions: 1756, smoke_assertions: 4995, migrations: 40,
                                 postgres_version: "15.2", clean_checkout: true, wiped_dist: true}
          verdicts: {maker_checker_sod: PASS, delegation: PASS, escalation: PASS, m21_intake: PASS,
                     m23_boundary: PASS, tenancy_rls: PASS, privacy: PASS, idempotency_concurrency: PASS,
                     contamination: CLEAN}
          adrs: [ADR-097, ADR-098, ADR-099, ADR-100]
          docs: [docs/04-modules/APPROVAL_WORKFLOW.md,
                 docs/build/stages/STAGE_3_M22_APPROVAL_COMPLETION.md,
                 docs/build/stages/STAGE_3_M22_APPROVAL_CERTIFICATION.md]
          # certification_pr recorded once the docs-only cert PR is opened.
      - module: m23-finance-integration
        capability: Finance integration, QA & release
        status: implemented   # Stage 3 — FRAMEWORK ONLY / POST-MVP foundation built on branch feature/stage-3-m23-finance-integration; 8 FORCE-RLS tables, 12 FIN_INTEGRATION_ audit codes, NO API/permission/event surface, NO second outbox; dispatch never calls out (ADR-096/101). Smoke + DB (PostgreSQL 16 CI) to certify post-merge.
        mvp: false
        db_ownership: [integration_destination(versioned, one enabled per system_code+scope; secret_reference pointer only, no secret cols),
                       integration_destination_history(append-only), integration_config(versioned, immutable-after-publish, idempotency-keyed;
                       bounded retry defaults; allow-list always on), integration_execution(governed execution aggregate; Framework-Only lifecycle;
                       opaque m21/m22 refs; bigint minor-unit evidence never transformed; no-dispatch-without-approval + bounded-retry CHECKs),
                       integration_execution_history(append-only), integration_attempt(append-only; framework_only always true; bounded attempt_no),
                       external_reference(append-only ack/failure/correlation mapping), integration_idempotency(append-only; unique per key)]
        api_ownership: []   # naming-map authoritative: NO API surface (Framework-Only internal library)
        event_ownership: []   # NO event family; publishes NO events; owns NO outbox
        shared_services_consumed: [kernel, m01, m03]   # m19/m20/m21/m22 referenced by OPAQUE id only (no package dep, no table reads); m33 deferred
        emits_events: []
        consumes_events: []
        permissions_required: []   # naming-map authoritative: NO permission namespace; RBAC-gated API deferred to the proven-integration phase (ADR-101)
        audit_prefix: FIN_   # FIN_INTEGRATION_ codes; shared with m19 (ADR-079), non-colliding
        hard_rules: [framework_only, secret_references_only, no_credential_columns, no_monetary_transformation, bounded_retry, idempotency, optimistic_concurrency, no_second_outbox_workflow_timer_notification_engine, no_external_call]
        boundary: "Records the GOVERNED integration execution of already-approved posting intents (opaque m21 posting-request + m22 approval refs)
                   against a configured external destination, FRAMEWORK ONLY — dispatch NEVER calls out (no connector; ADR-096/101), destinations
                   hold secret REFERENCES only (never credentials; ADR-102), money is opaque bigint evidence never transformed. OWNS no journals/
                   posting requests (m21), approval decisions (m22), GL/bank reconciliation (m20/m15), chart of accounts (m19), payments/AR/AP/
                   treasury or AI (m27); NO API/permission/event surface; NO second outbox/workflow/timer/notification engine. m33 (Integration
                   Foundation, phase 6) is an unbuilt dependency — a real connector is deferred behind the DispatchPort until proven."
        testing_required: [smoke, db_spec]
        tests_present: [m23-finance-integration.smoke, m23-finance-integration.db-spec, m23-services.db-spec]
        divergence_note: "module-registry reference_tables was a 3-table placeholder (m23 was never spec'd); actual Framework-Only foundation is 8 tables (ADR-101). naming-map's empty api/permission/event namespaces are intentionally preserved (no unproven surface claimed)."
        adrs: [ADR-101, ADR-102]
        certification_3_m23:
          module: m23-finance-integration
          stage: "3"
          branch: feature/stage-3-m23-finance-integration
          certification_branch: cert/stage-3-m23-finance-integration
          parent_baseline: 2f91fe2d0b4b9cf86b621a2bc8a602440a40f556   # main after governance PR #49 (m23 approved_for_build)
          implementation_pr: 50
          implementation_head: 0424ffac9da3ee4dfad1f5873bbe0c02ad48cacf   # reviewed head
          implementation_merge_sha: 9b8bec95ee35f386ae629a40bceb3e120d6579b7   # squash merge onto main (1 parent)
          merge_status: merged
          merged_at: "2026-08-04T14:08:36Z"
          tree_identical_to_reviewed_head: true   # git diff 0424ffa 9b8bec9 is empty (byte-identical)
          ci_implementation: passed   # run 30916718937 head 0424ffa: Smoke + DB (PostgreSQL 16) both success
          ci_main_push: passed        # main push 9b8bec9: Smoke + DB (PostgreSQL 16) both success
          smoke_certification: passed_locally         # 27 suites, 5071 assertions, 0 failures (m23 smoke 51, conformance 2782)
          postgresql_certification: passed_locally     # 56 db specs, 1808 assertions, 0 failures (PostgreSQL 15.2 throwaway; m23 db-spec 28 + m23-services 24)
          lint: passed                                 # 0 errors (67 pre-existing baseline warnings; m23 adds none)
          certification_status: certified_on_branch    # cert PR pending merge
          framework_only: true
          local_reverification: {tables_force_rls: 8, tenant_isolation_policies: 8, delete_grants: 0,
                                 append_only_update_grants: 0, float_columns: 0, secret_value_columns: 0,
                                 amount_minor_bigint: true, audit_codes: 12, gov_check_constraints: 7,
                                 one_enabled_dest_index: true, outboxes: 1, db_specs: 56, db_assertions: 1808,
                                 smoke_assertions: 5071, migrations: 42, postgres_version: "15.2",
                                 clean_checkout: true, wiped_dist: true}
          verdicts: {security: PASS, framework_only: PASS, secret_reference: PASS, m21_boundary: PASS,
                     m22_boundary: PASS, m33_boundary: PASS, privacy: PASS, tenancy: PASS, contamination: CLEAN}
          docs: [docs/04-modules/FINANCE_INTEGRATION.md,
                 docs/build/stages/STAGE_3_M23_FINANCE_INTEGRATION_COMPLETION.md,
                 docs/build/stages/STAGE_3_M23_FINANCE_INTEGRATION_CERTIFICATION.md]
          # certification_pr recorded once the docs-only cert PR is opened.
          stage_3_finance: complete   # m19 -> m15/m15a -> m20 -> m21 -> m22 -> m23 all implemented; m22/m23 certified_on_branch

  - stage: 4
    name: Legal
    status: documented
    priority: P1
    mvp: partial
    mvp_note: Read-only legal portfolio in MVP; write/convert/file DEFERRED.
    depends_on_stage: 2
    modules:
      - module: m14-legal
        capability: Enterprise Legal Matter Management (matter as a first-class object)
        status: implemented   # Stage 4.1 — merged (PR #24) + certified_on_branch (cert PR pending); see certification_4_1
        mvp: partial
        db_ownership: [legal_matter_type(versioned spec, one ACTIVE), legal_sla_policy(versioned spec, one ACTIVE),
                       legal_jurisdiction(configurable reference), legal_matter(core aggregate; SENSITIVE fields;
                       25-state lifecycle), legal_case_conversion(M13 idempotency ledger; one matter per source case),
                       legal_matter_status_history(append-only), legal_assignment_history(append-only),
                       legal_instruction(controlled accept/reject), legal_party, legal_activity(also correspondence),
                       legal_task, legal_issue, legal_position(SENSITIVE strategy), legal_opinion(SENSITIVE),
                       legal_research_reference, legal_pleading(m09 refs), legal_court_event, legal_deadline,
                       legal_external_counsel, legal_counsel_report(append-only), legal_cost_reference(finance refs),
                       legal_settlement(maker-checker + SoD CHECK; SENSITIVE terms), legal_outcome(append-only),
                       legal_note(append-only; confidential/privileged), legal_relationship]
        api_ownership: [/api/v1/legal]
        event_ownership: [legal.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06, m07, m08, m09, m13]
        consumes_events: [case.converted_to_matter (from m13; fire-and-forget, idempotent)]
        emits_events: [legal.lifecycle (36 event types)]
        permissions_required: [legal.*]
        hard_rules: [privilege, ethical_walls]
        testing_required: [smoke, db_spec, services_db_spec, api_http_spec]
        tests_present: [m14-legal.smoke (60), m14-legal.db-spec (40), m14-services.db-spec (30),
                        api-legal.db-spec (23)]
        # Enterprise, configurable, multi-tenant legal-matter platform (matter as a first-class object, not a
        # read-only view): matters created from M13 case conversion or direct legal intake; instructions,
        # parties, activities/correspondence, tasks, issues, legal positions/strategy, opinions, research,
        # pleadings, court events, deadlines (incl. limitation), external counsel + reports, costs, settlements
        # (maker-checker), judgments/outcomes, appeal + enforcement tracking. NOTHING Aptic-/Kenya-specific:
        # matter types, jurisdictions, forums, SLA policies are CONFIGURABLE. Full lifecycle: intake -> open ->
        # assign -> work -> resolve -> rule-gated close -> reopen/archive (25 states). Reuses m06 workflow,
        # m07 rules, m08 escalation+notifications, m09 documents/evidence THROUGH EVENTS/CONTRACTS + PORTS only
        # (no duplicate engines). SLA + deadlines + limitation are deterministic via a Clock port. Settlement
        # approval is maker-checker (proposer != approver, DB CHECK). Costs/exposure/enforcement store finance
        # REFERENCES only (no ledger/posting/payment/tax/reconciliation). Legal positions/strategy, opinions,
        # privileged notes, party contacts + confidential settlement terms are sensitive: RLS-stored, redacted
        # on read, NEVER in events/audit. M13 conversion = consume case.converted_to_matter, exactly one matter
        # per source case (legal_case_conversion ledger), never reads m13-owned tables. Publishes legal.lifecycle
        # (36 event types) through the ONE m06 outbox. No M16/M17/M18 internals, no finance, no AI decisioning,
        # no production court/telephony/messaging integrations (deferred behind ports).
        certification_4_1:
          module: m14-legal
          substage: "4.1"
          branch: feature/stage-4-1-m14-legal-matters
          certification_branch: cert/stage-4-1-m14-legal-matters
          parent_baseline: 12628451a9b4427724d08adbca8b9d626160aa92   # certified Stage 3.2 main (cert PR #23 merge)
          # Merged 2026-07-27 via PR #24 (squash) onto certified Stage 3.2 main; post-merge certification on branch.
          implementation_status: merged
          implementation_pr: 24
          implementation_head: baf4c71330a894fba378bb24a908b622fb68d8cf
          implementation_merge_sha: 41e7bf42ce8ba9b2cd02d50a9af73e8bb70d30a2
          merge_status: merged
          ci_implementation: passed                      # PR #24 head baf4c71 + main push 41e7bf4: Smoke + DB (PostgreSQL 16) both green
          smoke_certification: passed_locally            # 18 suites, 3092 assertions, 0 failures (incl. m14 smoke 60 + conformance 1448)
          postgresql_certification: passed_locally       # 31 db specs, 876 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                        # cert PR #25 head f7a3c62 + main push b6660a0: Smoke + DB (PostgreSQL 16) both green
          certification_status: certified                 # cert PR #25 merged 2026-07-27 → main b6660a0
          certification_pr: 25
          certification_merge_sha: b6660a037a6e25717e0b7a48e6d9baf9bde3d9e0
          certified_baseline_sha: b6660a037a6e25717e0b7a48e6d9baf9bde3d9e0   # merged main SHA (cert PR #25) — certified Stage 4.1 baseline
          tree_identical_to_reviewed_head: true          # git diff baf4c71 41e7bf4 is empty (byte-identical)
          local_reverification: {smoke_suites: 18, smoke_assertions: 3092, conformance_assertions: 1448,
                                 db_specs: 31, db_assertions: 876, migrations: 24, postgres_version: "15.2",
                                 clean_checkout: true, tree_identical_to_ci_head: true}
          adrs: [ADR-061, ADR-062, ADR-063, ADR-064]
          docs: [docs/build/stages/STAGE_4_1_M14_LEGAL_ARCHITECTURE.md,
                 docs/build/stages/STAGE_4_1_M14_LEGAL_READINESS.md,
                 docs/build/stages/STAGE_4_1_M14_LEGAL_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_4_1_M14_LEGAL_COMPLETION.md,
                 docs/build/stages/STAGE_4_1_M14_LEGAL_CERTIFICATION.md]
          # certification_pr recorded once the docs-only cert PR is opened.
      - module: m16-litigation
        capability: Enterprise Litigation Management (proceeding as a first-class object)
        status: implemented   # Stage 4.2 — merged (PR #26) + certified_on_branch (cert PR pending); see certification_4_2
        mvp: false
        db_ownership: [litigation_proceeding_type(versioned spec, one ACTIVE), litigation_sla_policy(versioned spec,
                       one ACTIVE), litigation_proceeding(core aggregate; SENSITIVE fields; 30-state lifecycle),
                       litigation_referral(M14 idempotency ledger; one proceeding per referral key),
                       litigation_status_history(append-only), litigation_assignment_history(append-only),
                       litigation_party, litigation_claim, litigation_filing(maker-checker SoD CHECK),
                       litigation_service(single-winner verify), litigation_appearance,
                       litigation_proceeding_record(append-only), litigation_witness, litigation_expert,
                       litigation_exhibit(single-winner admission), litigation_bundle(maker-checker SoD CHECK),
                       litigation_bundle_item, litigation_order(append-only), litigation_compliance_obligation,
                       litigation_outcome(append-only), litigation_appeal(one-active), litigation_deadline,
                       litigation_cost_reference, litigation_note(append-only; confidential/privileged),
                       litigation_relationship]
        api_ownership: [/api/v1/litigation]
        event_ownership: [litigation.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06, m07, m08, m09, m14]
        consumes_events: [M14 matter referral (governed inbound contract; fire-and-forget, idempotent)]
        emits_events: [litigation.lifecycle (36 event types)]
        permissions_required: [litigation.*]
        hard_rules: [privilege, ethical_walls, maker_checker]
        testing_required: [smoke, db_spec, services_db_spec, api_http_spec]
        tests_present: [m16-litigation.smoke (71), m16-litigation.db-spec (41), m16-services.db-spec (40),
                        api-litigation.db-spec (21)]
        # Enterprise, configurable, multi-tenant litigation & adjudicative-proceedings platform (a proceeding as a
        # first-class object, not a court-event on a matter): proceedings before configurable forums (courts /
        # tribunals / arbitration / mediation / regulatory / disciplinary), created from an M14 matter referral or
        # direct litigation intake; parties, claims, filings, service of process, appearances/diary, hearing
        # records, witnesses, experts, exhibits, hearing bundles, orders, compliance obligations, rulings/judgments,
        # appeals, litigation deadlines (incl. limitation), cost references. NOTHING Kenya-specific: proceeding
        # types, jurisdictions, forums, SLA policies are CONFIGURABLE. Full lifecycle: draft -> referred ->
        # under_review -> filed -> served -> pleadings -> hearing -> decision -> concluded -> closed, 30 states with
        # reopen/stayed/settled/withdrawn/dismissed/archived. Reuses m06 workflow, m07 rules, m08 escalation+
        # notifications, m09 documents/bundles THROUGH EVENTS/CONTRACTS + PORTS only (no duplicate engines). SLA +
        # deadlines + limitation are deterministic via a Clock port. Filing + bundle approval are maker-checker
        # (approver != preparer, DB CHECK); service verification + exhibit admission are single-winner CAS. Costs
        # store court + finance REFERENCES only (no ledger/posting/payment/tax/reconciliation). Legal strategy, full
        # pleadings, witness statements, full submissions, private witness/party contacts + confidential order/
        # outcome terms are sensitive: RLS-stored, redacted on read, NEVER in events/audit. M14 referral = consume a
        # governed inbound contract, exactly one proceeding per referral key (a matter may have several
        # proceedings), never reads m14-owned tables. m17 enforcement + m18 knowledge are DOWNSTREAM boundaries
        # reached by safe events only (EnforcementReferralReady / KnowledgeCandidateCreated). Publishes
        # litigation.lifecycle (36 event types) through the ONE m06 outbox. No M17/M18 internals, no finance, no AI,
        # no production e-filing/court-scraping/calendar/telephony/messaging (deferred behind ports).
        certification_4_2:
          module: m16-litigation
          substage: "4.2"
          branch: feature/stage-4-2-m16-litigation-management
          certification_branch: cert/stage-4-2-m16-litigation-management
          parent_baseline: b6660a037a6e25717e0b7a48e6d9baf9bde3d9e0   # certified Stage 4.1 main (cert PR #25 merge)
          # Merged 2026-07-28 via PR #26 (squash) onto certified Stage 4.1 main; post-merge certification on branch.
          implementation_status: merged
          implementation_pr: 26
          implementation_head: 3b629fd77dfd1d44082e3682230c6a29f2830435
          implementation_merge_sha: de3bc46bd556b1179933488c2511d1f67845281e
          merge_status: merged
          ci_implementation: passed                      # PR #26 head 3b629fd + main push de3bc46: Smoke + DB (PostgreSQL 16) both green
          smoke_certification: passed_locally            # 19 suites, 3401 assertions, 0 failures (incl. m16 smoke 71 + conformance 1680)
          postgresql_certification: passed_locally       # 34 db specs, 978 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                        # cert PR #27 head 7f4dcee + main push b959298: Smoke + DB (PostgreSQL 16) both green
          certification_status: certified                 # cert PR #27 merged 2026-07-28 → main b959298
          certified_baseline_sha: b959298cee18601b3768f72285062f776bbc35cb   # merged main SHA (cert PR #27) — certified Stage 4.2 baseline
          tree_identical_to_reviewed_head: true          # git diff 3b629fd de3bc46 is empty (byte-identical)
          local_reverification: {smoke_suites: 19, smoke_assertions: 3401, conformance_assertions: 1680,
                                 db_specs: 34, db_assertions: 978, migrations: 26, postgres_version: "15.2",
                                 clean_checkout: true, tree_identical_to_ci_head: true}
          certification_pr: 27
          certification_merge_sha: b959298cee18601b3768f72285062f776bbc35cb
          adrs: [ADR-065, ADR-066, ADR-067, ADR-068]
          docs: [docs/build/stages/STAGE_4_2_M16_LITIGATION_ARCHITECTURE.md,
                 docs/build/stages/STAGE_4_2_M16_LITIGATION_READINESS.md,
                 docs/build/stages/STAGE_4_2_M16_LITIGATION_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_4_2_M16_LITIGATION_COMPLETION.md,
                 docs/build/stages/STAGE_4_2_M16_LITIGATION_CERTIFICATION.md]
      - module: m17-recovery
        capability: Enterprise Recovery & Enforcement (recovery as a first-class object)
        status: implemented   # Stage 4.3 — implemented_on_branch (not merged/certified); see certification_4_3
        mvp: false
        db_ownership: [recovery_type(versioned spec, one ACTIVE), recovery_sla_policy(versioned spec, one ACTIVE),
                       recovery_case(core aggregate; SENSITIVE fields; 29-state lifecycle),
                       recovery_referral(M16 idempotency ledger; one recovery per referral key),
                       recovery_status_history(append-only), recovery_assignment_history(append-only),
                       recovery_party(debtors/liable parties), recovery_instrument(enforceable instruments),
                       recovery_strategy(append-only), recovery_demand, recovery_negotiation,
                       recovery_arrangement(maker-checker SoD CHECK), recovery_installment(schedule metadata only),
                       recovery_enforcement_action, recovery_security, recovery_agent,
                       recovery_agent_report(append-only), recovery_receipt(append-only; REFERENCE only),
                       recovery_waiver(append-only), recovery_writeoff_recommendation(maker-checker SoD CHECK),
                       recovery_outcome(append-only), recovery_deadline, recovery_cost_reference,
                       recovery_note(append-only; confidential/privileged), recovery_relationship]
        api_ownership: [/api/v1/recovery]
        event_ownership: [recovery.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06, m07, m08, m09, m14, m16]
        consumes_events: [M16 enforcement referral (governed inbound contract; fire-and-forget, idempotent)]
        emits_events: [recovery.lifecycle (36 event types)]
        permissions_required: [recovery.*]
        hard_rules: [privilege, ethical_walls, maker_checker, finance_boundary]
        testing_required: [smoke, db_spec, services_db_spec, api_http_spec]
        tests_present: [m17-recovery.smoke (72), m17-recovery.db-spec (41), m17-services.db-spec (34),
                        api-recovery.db-spec (22)]
        # Enterprise, configurable, multi-tenant recovery & enforcement platform (a recovery as a first-class
        # object): operational tracking of recoveries from judgments/decrees/orders/awards/settlements/defaults/
        # guarantees/securities, created from an M16 enforcement referral or direct recovery intake; debtors/liable
        # parties, enforceable instruments, strategy, demands, negotiations, operational repayment arrangements +
        # installment schedules, enforcement actions (attachment/garnishee/execution/auction/repossession refs),
        # security/collateral references, external recovery agents + reports, recovery receipt REFERENCES, waivers,
        # write-off RECOMMENDATIONS, outcomes. NOTHING Kenya-specific: recovery types, jurisdictions, procedures,
        # notices, enforcement methods are CONFIGURABLE. Full lifecycle: draft -> referred -> strategy -> demand/
        # negotiation -> arrangement/enforcement -> security/agent -> receipt/waiver/write-off -> outcome ->
        # resolved -> closed, 29 states with reopen/suspended/settled/uncollectible/withdrawn/archived. Reuses m06
        # workflow, m07 rules, m08 escalation+notifications, m09 documents THROUGH EVENTS/CONTRACTS + PORTS only (no
        # duplicate engines). SLA + deadlines + limitation are deterministic via a Clock port. Arrangement + write-
        # off approval are maker-checker (approver != proposer/recommender, DB CHECK). FINANCE BOUNDARY (ADR-071):
        # ALL amounts are REFERENCES only — NO cash application, NO general ledger, NO accounts receivable, NO
        # payment execution, NO reconciliation, NO accounting write-off. Write-off is a RECOMMENDATION with maker-
        # checker approval; receipts carry an external/finance reference; recovered/outstanding tallies are
        # reference figures. Debtor contacts, negotiation strategy, settlement terms, bank/payment details and
        # security valuations are sensitive: RLS-stored, redacted on read, NEVER in events/audit (ADR-072). M16
        # referral = consume a governed inbound contract, exactly one recovery per referral key (a proceeding may
        # produce several), never reads m16/m14-owned tables. Publishes recovery.lifecycle (36 event types) through
        # the ONE m06 outbox. No M18 internals, no finance/GL/AR/payment/reconciliation, no AI, no production
        # e-auction/court/telephony/messaging (deferred behind ports).
        certification_4_3:
          module: m17-recovery
          substage: "4.3"
          branch: feature/stage-4-3-m17-recovery-enforcement
          certification_branch: cert/stage-4-3-m17-recovery-enforcement
          parent_baseline: b959298cee18601b3768f72285062f776bbc35cb   # certified Stage 4.2 main (cert PR #27 merge)
          governance_approval_pr: 28                     # m17-recovery deferred -> approved_for_build (PR #28, main fd594ab)
          # Merged 2026-07-28 via PR #29 (squash) onto governance-approved main fd594ab; post-merge certification on branch.
          implementation_status: merged
          implementation_pr: 29
          implementation_head: 311757c2824c1ff2eeac2128af5ec3ff365d953e
          implementation_merge_sha: 452376d1787b742aa6a1786934ed16ee3c954ea1
          merge_status: merged
          ci_implementation: passed                      # PR #29 head 311757c + main push 452376d: Smoke + DB (PostgreSQL 16) both green
          smoke_certification: passed_locally            # 20 suites, 3709 assertions, 0 failures (incl. m17 smoke 72 + conformance 1910)
          postgresql_certification: passed_locally       # 37 db specs, 1075 assertions, 0 failures (PostgreSQL 15.2 throwaway)
          ci_certification: passed                        # cert PR #30 CI green; merged to main as 5aec639
          certification_status: certified                 # cert PR #30 merged 2026-07-29 onto main (5aec639)
          certified_baseline_sha: 452376d1787b742aa6a1786934ed16ee3c954ea1   # merged main SHA (PR #29) tested for certification
          tree_identical_to_reviewed_head: true          # git diff 311757c 452376d is empty (byte-identical)
          local_reverification: {smoke_suites: 20, smoke_assertions: 3709, conformance_assertions: 1910,
                                 db_specs: 37, db_assertions: 1075, migrations: 28, postgres_version: "15.2",
                                 clean_checkout: true, tree_identical_to_ci_head: true}
          certification_pr: 30
          certification_merge_sha: 5aec639571af370e9570a0316ab48d461c1b6b2d
          adrs: [ADR-069, ADR-070, ADR-071, ADR-072]
          docs: [docs/build/stages/STAGE_4_3_M17_RECOVERY_ARCHITECTURE.md,
                 docs/build/stages/STAGE_4_3_M17_RECOVERY_READINESS.md,
                 docs/build/stages/STAGE_4_3_M17_RECOVERY_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_4_3_M17_RECOVERY_COMPLETION.md,
                 docs/build/stages/STAGE_4_3_M17_RECOVERY_CERTIFICATION.md]
      - module: m18-legaldocs
        capability: Enterprise Legal Knowledge Management (legal knowledge as a first-class, governed corpus)
        status: implemented   # Stage 4.4 — implemented_on_branch (not merged/certified); see certification_4_4
        mvp: false
        db_ownership: [legaldoc_taxonomy(configurable practice-area/jurisdiction/legal-topic/document-type/tag;
                       one-active per kind+code), legaldoc_knowledge(core aggregate; SENSITIVE abstract; 9-state
                       PUBLISHABLE lifecycle; versioned via supersession; one published per code; SoD CHECK;
                       idempotency-keyed), legaldoc_status_history(append-only), legaldoc_assignment_history(append-only),
                       legaldoc_authority(statutes/case-law/regulations/opinions), legaldoc_authority_treatment(append-only;
                       followed/distinguished/overruled/...), legaldoc_precedent(SENSITIVE holding),
                       legaldoc_opinion(PRIVILEGED; maker-checker SoD CHECK), legaldoc_research(SENSITIVE findings),
                       legaldoc_template(versioned, immutable-after-publish, one-published, SoD CHECK),
                       legaldoc_clause(versioned clause library, immutable-after-publish, one-published, SoD CHECK),
                       legaldoc_clause_relation(self-edge CHECK; active-unique), legaldoc_reference(OPAQUE cross-module
                       ids for m09 doc/m14 matter/m16 litigation/m17 recovery/authority/precedent/external),
                       legaldoc_relationship(self-edge CHECK; active-unique), legaldoc_review(deterministic
                       review/expiry deadlines via Clock port), legaldoc_tag(one-active per kind+code; soft-delete, no
                       DELETE), legaldoc_citation, legaldoc_note(append-only; confidential/privileged/strategy),
                       legaldoc_usage(append-only analytics), legaldoc_approval_history(append-only maker-checker evidence)]
        api_ownership: [/api/v1/legaldocs]
        event_ownership: [legaldocs.lifecycle]
        shared_services_consumed: [kernel, m01, m02, m03, m06, m07, m08, m09, m14, m16, m17]
        emits_events: [legaldocs.lifecycle (36 event types)]
        permissions_required: [legaldocs.*]
        hard_rules: [privilege, ethical_walls, maker_checker, no_ai_search, no_cross_module_mutation]
        testing_required: [smoke, db_spec, services_db_spec, api_http_spec]
        tests_present: [m18-legaldocs.smoke (74), m18-legaldocs.db-spec (41), m18-services.db-spec (163),
                        api-legaldocs.db-spec (22)]
        # Enterprise, configurable, multi-tenant legal KNOWLEDGE platform (legal knowledge as a first-class corpus):
        # legal knowledge records (memos/practice-notes/guidance/checklists/interpretations/advisories/playbooks/
        # standard-positions), legal authorities + citations + case-law/statutory treatments, precedents, PRIVILEGED
        # legal opinions (maker-checker approval), research notes, versioned document TEMPLATES + a CLAUSE library
        # (maker-checker, immutable-after-publish), configurable taxonomy/classification, keyword search (NO AI /
        # vector / semantic / embeddings), supersession-based versioning, review/expiry deadlines, cross-module
        # references. NOTHING jurisdiction-specific: practice areas, jurisdictions, legal topics, document types and
        # tags are CONFIGURABLE per tenant (ADR-073). One shared 9-state PUBLISHABLE lifecycle (draft -> under_review
        # -> changes_requested -> approved -> published -> superseded/withdrawn -> archived, with reopened) for
        # knowledge/template/clause, single choke point, maker-checker SoD (approver != submitter/author, DB CHECK).
        # Published content is IMMUTABLE — a change is a new version via supersession; content_hash frozen at publish;
        # one published per code (ADR-074). Deterministic review/expiry math via a Clock port. Reuses m06 workflow,
        # m07 rules, m08 escalation+notifications, m09 documents THROUGH EVENTS/CONTRACTS + PORTS only (no duplicate
        # engines). OWNS NO STORAGE — document bytes live in m09; m18 stores references. Cross-module references (m09
        # documents, m14 matters, m16 litigation, m17 recoveries) are OPAQUE ids — m18 reads no other module's tables
        # and mutates nothing cross-module (ADR-075). Privileged legal advice, confidential clause/opinion/analysis
        # text, drafting strategy and restricted notes are sensitive: RLS-stored, redacted on read, gated by dedicated
        # privileged permissions, and NEVER placed in events/audit/logs/analytics/search snippets (ADR-076); ethical
        # walls via RLS FORCE. Publishes legaldocs.lifecycle (36 event types) through the ONE m06 outbox. No finance/
        # accounting/ledger/AR/payment/reconciliation, no AI, no portals, no production court or legal-research vendor
        # ingestion (deferred behind ports).
        certification_4_4:
          module: m18-legaldocs
          substage: "4.4"
          branch: feature/stage-4-4-m18-legal-knowledge-management
          certification_branch: cert/stage-4-4-m18-legal-knowledge-management
          parent_baseline: f3f053df94f5a9cc0ddb27a214a2c2c8427d0c1d   # governance-approved main (PR #31 merge)
          governance_approval_pr: 31                     # m18-legaldocs documented -> approved_for_build (PR #31, main f3f053d)
          # Merged 2026-07-29 via PR #32 (squash) onto governance-approved main f3f053d; post-merge certification on branch.
          implementation_status: merged
          implementation_pr: 32
          implementation_head: 2168d83065b2221191f8d9cbf9e4ed1060476c8d
          implementation_merge_sha: 9bf1a0ce69a7fe75deffedfd5d0d859b2cb05755
          merge_status: merged
          ci_implementation: passed                      # PR #32 head 2168d83 + merge 9bf1a0c: Smoke lane + DB lane (PostgreSQL 16) both green
          smoke_certification: passed_locally            # 21 suites, 3995 assertions, 0 failures (incl. m18 smoke 74 + conformance 2116)
          postgresql_certification: passed_locally       # 40 db specs, 1301 assertions, 0 failures; fresh replay of 30 migrations (PostgreSQL 15.2 throwaway)
          ci_certification: pending                       # cert PR CI recorded once green (see certification report §14)
          certification_status: certified_on_branch      # cert PR pending, not merged (stopped before merge)
          certified_baseline_sha: 9bf1a0ce69a7fe75deffedfd5d0d859b2cb05755   # merged main SHA (PR #32) tested for certification
          tree_identical_to_reviewed_head: true          # git diff 2168d83 9bf1a0c is empty (byte-identical)
          local_reverification: {smoke_suites: 21, smoke_assertions: 3995, conformance_assertions: 2116,
                                 db_specs: 40, db_assertions: 1301, migrations_replayed: 30, postgres_version: "15.2",
                                 clean_checkout: true, tree_identical_to_ci_head: true,
                                 m18_tables: 20, force_rls: 20, composite_pk: 20, delete_grants: 0,
                                 append_only_ledgers: 6, composite_fks: 13, single_col_fks: 0, sod_checks: 4,
                                 selfedge_checks: 2, version_checks: 13, one_published_indexes: 3,
                                 permissions: 46, privileged_permissions: 19, audit_codes: 55,
                                 registered_code_count: 486, event_types: 36, event_families: 15,
                                 mutating_routes: 48, read_routes: 21, lifecycle_states: 9, outbox_tables: 1}
          certification_pr: pending
          certification_merge_sha: pending
          adrs: [ADR-073, ADR-074, ADR-075, ADR-076]
          docs: [docs/build/stages/STAGE_4_4_M18_LEGALDOCS_ARCHITECTURE.md,
                 docs/build/stages/STAGE_4_4_M18_LEGALDOCS_READINESS.md,
                 docs/build/stages/STAGE_4_4_M18_LEGALDOCS_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_4_4_M18_LEGALDOCS_COMPLETION.md,
                 docs/build/stages/STAGE_4_4_M18_LEGALDOCS_CERTIFICATION.md]

  - stage: 5
    name: AI layer
    status: documented
    priority: P1
    mvp: partial
    mvp_note: Governed summaries/suggestions only; no AI-executed controlled actions ever.
    depends_on_stage: 2
    requires_review: [approved_ai_providers_and_routing, data_residency, ai_quotas]
    modules:
      - module: m24-ai-foundation
        capability: AI gateway, model/prompt registries, RAG/vector, governance
        status: implemented   # governance/approve-m24-ai-foundation -> Stage 5 M24 built (feature/stage-5-m24-ai-foundation). 17 owned tables (ai_provider/model/prompt/dlp_policy/config aggregates + append-only request/output/citation/human_review/usage/cost/routing/dlp_finding/event ledgers); 23 ai.* permissions, 22 AI_ audit codes, 3 event families (ai.request_lifecycle/ai.output_lifecycle/ai.governance_lifecycle). Provider abstraction = deterministic test doubles ONLY (no production adapters, no secrets, no network, no lock-in per governance). GOVERNED: human-in-the-loop enforced 3 layers (evaluateApprovalGate engine + ReviewService + DB ai_output_human_ck), confidence bps + citations, DLP-before-routing (restricted/confidential -> approved provider only, fail closed), NO autonomous completion. Large I/O via m09 doc refs. Deps m02/m03/m06/m09 certified; m41-security deferred behind DlpPolicyEvaluator port (ADR-105), integrate when built. RAG/vector deferred per spec MVP (ADR-106). PG16-compatible. mvp_note honored: governed summaries/suggestions only, no AI-executed controlled actions ever. Naming: /api/v1/ai, ai.*, AI_ audit, 3 families (registered).
        mvp: partial
        shared_services_consumed: [m02, m03, m06, m09, m41]
        permissions_required: [ai.*]
        hard_rules: [human_in_the_loop, confidence_and_citations, dlp, no_restricted_data_to_unapproved_providers]
        testing_required: [smoke, db_spec]
        certification_5_m24:
          module: m24-ai-foundation
          stage: "5"
          branch: feature/stage-5-m24-ai-foundation
          certification_branch: cert/stage-5-m24-ai-foundation
          parent_baseline: ab686cd40ed0827b9e3499b2e8d094c522014b44   # main after governance PR #55 (m24 approved_for_build)
          implementation_pr: 56
          implementation_head: dfa84da05eb5d7ac65a39a91bca18d4ca8c4335e   # reviewed head
          implementation_merge_sha: 22c3e032c2092b5c754942bf4027dd933501e311   # squash merge onto main (1 parent ab686cd)
          merge_status: merged
          merged_at: "2026-08-05T08:21:20Z"
          tree_identical_to_reviewed_head: true   # git diff dfa84da 22c3e03 is empty (byte-identical)
          ci_implementation: passed   # head dfa84da: Smoke + DB (PostgreSQL 16) both success
          ci_main_push: passed        # main push 22c3e03: Smoke + DB (PostgreSQL 16) both success
          smoke_certification: passed_locally         # 29 suites, 5403 assertions, 0 failures (m24 smoke 73, conformance 2994)
          postgresql_certification: passed_locally     # 60 db specs, 1923 assertions, 0 failures (PostgreSQL 15.2 throwaway; m24-ai-foundation 41 + m24-services 29 + all api-* green)
          lint: passed                                 # 0 errors (68 pre-existing baseline warnings; m24 adds none), wiped-dist
          certification_status: certified_on_branch    # cert PR pending merge
          governed_mvp: true
          local_reverification: {tables_force_rls: 17, tenant_isolation_policies: 17, delete_grants: 0,
                                 append_only_update_grants: 0, float_columns: 0, secret_value_columns: 0,
                                 cost_minor_bigint: true, confidence_bps_integer: true, ai_permissions: 23,
                                 ai_permissions_privileged: 10, gov_check_constraints: 10, outboxes: 1,
                                 db_specs: 60, db_assertions: 1923, smoke_assertions: 5403, migrations: 46,
                                 postgres_version: "15.2", clean_checkout: true, wiped_dist: true}
          verdicts: {no_autonomous_action: PASS, dlp_before_routing: PASS, approved_provider_routing: PASS,
                     human_review: PASS, citations: PASS, confidence_bps: PASS, usage_cost_no_float: PASS,
                     secret_reference: PASS, permissions: PASS, tenancy: PASS, privacy: PASS,
                     m41_boundary: PASS, single_outbox: PASS, contamination: CLEAN}
          docs: [docs/05-ai/ENTERPRISE_AI_FOUNDATION.md,
                 docs/build/stages/STAGE_5_M24_AI_FOUNDATION_COMPLETION.md,
                 docs/build/stages/STAGE_5_M24_AI_FOUNDATION_CERTIFICATION.md]
          # certification_pr recorded once the docs-only cert PR is opened.
          stage_5_ai: m24_foundation_certified_on_branch   # next: M25 AI Governance & Orchestration (depends on m24)
      - module: m25-operational-ai
        capability: Operational AI (feedback/case)
        status: implemented   # Stage 5 M25 built (feature/stage-5-m25-operational-ai). 9 owned ops_ai_* tables (4 mutable aggregates config/subject/analysis/suggestion + 5 append-only: 2 history + evidence/review/idempotency). 7 ai.operational.*/ai.suggestion.* permissions (3 privileged) in the SHARED ai.* namespace, 11 AI_OPS_* audit codes (shared AI_ prefix). NO new API root, NO new event family, NO 2nd outbox (naming-map authoritative). Consumes the M24 governed pipeline BY CONTRACT via AiGatewayPort (M24AiGateway wraps m24 RequestService/ReviewService — routing/DLP/confidence/citations/usage all in m24, never duplicated). RECOMMENDS ONLY: analysis + suggestion can only be decided by a HUMAN (evaluateDecisionGate + DB ops_ai_analysis_human_ck/ops_ai_suggestion_human_ck); config require_human_review always on + auto_apply always off (CHECKs); an ACCEPT is refused unless m24 approved the output; m25 never closes/escalates/reassigns a controlled m12/m13 item. Feedback/case/document/m24-request/output referenced by OPAQUE id (no cross-module FK; reads no m12/m13/m24 table). Confidence integer bps; no float; no secret column. Deps m24 (certified) + m12/m13 (certified). PG16-compatible. Spec docs/05-ai/OPERATIONAL_AI.md.
        mvp: partial
        shared_services_consumed: [m24, m12, m13]
        testing_required: [smoke, db_spec]
        certification_5_m25:
          module: m25-operational-ai
          stage: "5"
          branch: feature/stage-5-m25-operational-ai
          certification_branch: cert/stage-5-m25-operational-ai
          parent_baseline: decd561106c502ec4d008c6ca27f8965a82b9d62   # main after governance PR #58 (m25 approved_for_build)
          implementation_pr: 59
          implementation_head: 37e753587359b2440af6287ec3146368545f38c0   # reviewed head
          implementation_merge_sha: e7d29fa9a4165e5f502344b9e24b514259eb4b32   # squash merge onto main (1 parent decd561)
          merge_status: merged
          merged_at: "2026-08-05T11:45:43Z"
          tree_identical_to_reviewed_head: true   # git diff 37e7535 e7d29fa is empty (byte-identical)
          ci_implementation: passed   # head 37e7535: Smoke + DB (PostgreSQL 16) both success
          ci_main_push: passed        # main push e7d29fa: Smoke + DB (PostgreSQL 16) both success
          smoke_certification: passed_locally         # 30 suites, 5497 assertions, 0 failures (m25 smoke 57, conformance 3031)
          postgresql_certification: passed_locally     # 62 db specs, 1972 assertions, 0 failures (PostgreSQL 15.2 throwaway; m25-operational-ai 29 + m25-services 20 + all api-* green)
          lint: passed                                 # 0 errors (68 pre-existing baseline warnings; m25 adds none), wiped-dist
          certification_status: certified_on_branch    # cert PR pending merge
          governed_mvp: true
          local_reverification: {tables_force_rls: 9, tenant_isolation_policies: 9, delete_grants: 0,
                                 append_only_update_grants: 0, float_columns: 0, secret_columns: 0,
                                 confidence_bps_integer: true, ai_permissions: 7, gov_check_constraints: 4,
                                 outboxes: 1, db_specs: 62, db_assertions: 1972, smoke_assertions: 5497,
                                 migrations: 48, postgres_version: "15.2", clean_checkout: true, wiped_dist: true}
          verdicts: {no_autonomous_action: PASS, recommends_only: PASS, human_review: PASS, confidence_preserved: PASS,
                     m24_by_contract: PASS, m12_m13_boundary: PASS, no_duplicate_engine: PASS, single_outbox: PASS,
                     tenancy: PASS, privacy: PASS, m24_unmodified: PASS, contamination: CLEAN}
          docs: [docs/05-ai/OPERATIONAL_AI.md,
                 docs/build/stages/STAGE_5_M25_OPERATIONAL_AI_COMPLETION.md,
                 docs/build/stages/STAGE_5_M25_OPERATIONAL_AI_CERTIFICATION.md]
          # certification_pr recorded once the docs-only cert PR is opened.
          stage_5_ai: m25_operational_certified_on_branch   # next: M26 Credit/Legal AI (m26-legal-ai per BUILD_SEQUENCE)
      - module: m26-legal-ai
        capability: Legal AI
        status: implemented   # Stage 5 M26 built (feature/stage-5-m26-legal-ai). 11 owned legal_ai_* tables (4 mutable aggregates config/subject/analysis/suggestion + 7 append-only: analysis_history/finding/citation/suggestion_history/review/evidence/idempotency). 6 ai.legal.*/ai.privileged.* permissions (4 privileged) in the SHARED ai.* namespace; 14 AI_LEGAL_* audit codes (shared AI_ prefix). NO new API root, NO new event family, NO 2nd outbox (naming-map authoritative). Consumes the M24 governed pipeline BY CONTRACT via AiGatewayPort (M24AiGateway wraps m24 RequestService/ReviewService — routing/DLP/confidence/citations/usage in m24, never duplicated/bypassed). LEGAL-ADVISORY ONLY: analysis+suggestion decided only by a HUMAN (evaluateReviewGate + DB legal_ai_analysis_human_ck/legal_ai_suggestion_human_ck); citations-required accept needs a citation (legal_ai_analysis_cite_ck); ethical wall (privileged/work_product require ai.privileged.read, audited); an AI inference is 'inferred' never a verified fact (legal_ai_finding_factstatus_ck); config require_human_review always on + auto_apply always off. M14 matter/M09 doc/M24 request-output referenced by OPAQUE id (no cross-module FK; reads no m14/m09/m24 table; M14 stays legal source of truth; citations hold a doc REFERENCE+version/hash, never content). Confidence integer bps; no float; no secret column. Deps m24 (certified) + m14 (certified). PG16-compatible. Spec docs/05-ai/LEGAL_AI.md. mvp:false honored (NON-MVP; no autonomous legal conclusion/filing/settlement/enforcement).
        mvp: false
        shared_services_consumed: [m24, m14]
        testing_required: [smoke, db_spec]
        certification_5_m26:
          module: m26-legal-ai
          stage: "5"
          governance_pr: 61
          governance_merge_sha: 96bc00db0f63356fc981b809361b4a122b6925d1   # main after governance PR #61 (m26 approved_for_build)
          branch: feature/stage-5-m26-legal-ai
          certification_branch: cert/stage-5-m26-legal-ai
          parent_baseline: 96bc00db0f63356fc981b809361b4a122b6925d1
          implementation_pr: 62
          implementation_head: fccbf8c8bd87ed979cc1699b0baf6859f0b377c0   # reviewed head
          implementation_merge_sha: cbb8356c215eb15d35223d27ca285f0a0d6e3661   # squash merge onto main (1 parent 96bc00d)
          merge_status: merged
          merged_at: "2026-08-06T06:15:13Z"
          tree_identical_to_reviewed_head: true   # git diff fccbf8c cbb8356 is empty (byte-identical)
          ci_implementation: passed   # head fccbf8c: Smoke + DB (PostgreSQL 16) both success
          ci_main_push: passed        # main push cbb8356: Smoke + DB (PostgreSQL 16) both success
          smoke_certification: passed_locally         # 31 suites, 5609 assertions, 0 failures (m26 smoke 71, conformance 3072)
          postgresql_certification: passed_locally     # 64 db specs, 2029 assertions, 0 failures (PostgreSQL 15.2 throwaway; m26-legal-ai 33 + m26-services 24 + all api-* green)
          lint: passed                                 # 0 errors (68 pre-existing baseline warnings; m26 adds none), wiped-dist
          certification_status: certified_on_branch    # cert PR pending merge
          certification_head: pending                  # docs-only cert commit
          certification_merge_sha: pending
          governed_mvp: false
          local_reverification: {tables: 11, tables_force_rls: 11, tenant_isolation_policies: 11, delete_grants: 0,
                                 append_only_update_grants: 0, unsafe_tenant_fks: 0, float_columns: 0, secret_columns: 0,
                                 confidence_bps_integer: true, version_columns: 4, ai_permissions: 6, gov_check_constraints: 7,
                                 outboxes: 1, db_specs: 64, db_assertions: 2029, smoke_assertions: 5609, migrations: 50,
                                 audit_registry_total: 719, postgres_version: "15.2", clean_checkout: true, wiped_dist: true}
          verdicts: {m24_handoff: PASS, m14_boundary: PASS, m09_citation_boundary: PASS, privilege_confidentiality: PASS,
                     ethical_walls: PASS, dlp: PASS, confidence: PASS, citations: PASS, legal_findings: PASS,
                     suggestions: PASS, human_review: PASS, no_autonomous_legal_action: PASS, idempotency: PASS,
                     concurrency: PASS, tenancy_rls: PASS, privacy: PASS, event_family_reuse: PASS, api_absence: PASS,
                     m24_m14_m09_unmodified: PASS, contamination: CLEAN}
          docs: [docs/05-ai/LEGAL_AI.md,
                 docs/build/stages/STAGE_5_M26_LEGAL_AI_ARCHITECTURE.md,
                 docs/build/stages/STAGE_5_M26_LEGAL_AI_READINESS.md,
                 docs/build/stages/STAGE_5_M26_LEGAL_AI_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_5_M26_LEGAL_AI_COMPLETION.md,
                 docs/build/stages/STAGE_5_M26_LEGAL_AI_CERTIFICATION.md]
          # certification_pr recorded once the docs-only cert PR is opened.
          stage_5_ai: m26_legal_certified_on_branch   # next: M27 Finance AI (m27-finance-ai; NO auto-post) per BUILD_SEQUENCE
      - module: m27-finance-ai
        capability: Finance AI (recon suggestions; NO auto-post)
        status: implemented   # Stage 5 M27 built (feature/stage-5-m27-finance-ai); governance/approve-m27-finance-ai (documented->approved_for_build) merged as gov PR #64, then implemented. 12 owned finance_ai_* tables (4 mutable aggregates config/subject/analysis/suggestion + 8 append-only: analysis_history/model_result/exception_classification/suggestion_history/feature/evidence/review/idempotency). 5 ai.finance.* permissions (3 privileged) in the SHARED ai.* namespace; 13 AI_FINANCE_* audit codes (shared AI_ prefix; count 719->732). NO new API root, NO new event family, NO 2nd outbox (naming-map authoritative). Consumes the M24 governed pipeline BY CONTRACT via AiGatewayPort (M24AiGateway wraps m24 RequestService/ReviewService — routing/DLP/confidence/usage in m24, never duplicated/bypassed). HARD RULES [no_autopost, explainable_matches]: NEVER auto-posts/auto-matches/approves/mutates a finance record (DB finance_ai_config_autopost_ck + finance_ai_config_automatch_ck always false); analysis+suggestion decided only by a HUMAN (evaluateReviewGate + DB finance_ai_analysis_human_ck/finance_ai_suggestion_human_ck); an accepted explainability-required suggestion needs a matched FEATURE (finance_ai_suggestion_explain_ck — no unexplained match accepted); config require_human_review always on. M15/M20/M24 referenced by OPAQUE id (no cross-module FK; reads no m15/m20/m24 table; matching owned by m15a, GL recon by m20; M15/M20 = source of truth). Money bigint minor units (amount_minor ::text, never float, ADR-007); confidence integer bps; no secret column. A suggestion is never a confirmed accounting fact. Deps m24 (certified) + m15/m20 (implemented). PG16-compatible. Spec docs/05-ai/FINANCE_AI.md.
        mvp: partial
        shared_services_consumed: [m24, m15, m20]
        hard_rules: [no_autopost, explainable_matches]
        testing_required: [smoke, db_spec]
        certification_5_m27:
          module: m27-finance-ai
          stage: "5"
          governance_pr: 64
          governance_merge_sha: ccce6d6798c7bf2484d829e20b181ea88f98c7a1   # main after governance PR #64 (m27 approved_for_build)
          branch: feature/stage-5-m27-finance-ai
          certification_branch: cert/stage-5-m27-finance-ai
          parent_baseline: ccce6d6798c7bf2484d829e20b181ea88f98c7a1
          implementation_pr: 65
          implementation_original_tree: e59d8d1d89d4e6abe840aa55ec1cd548ec7449fb   # original push
          implementation_head: 8c87583076b8d09dde2f061c50760f859324aeda   # final reviewed head (empty CI-retrigger commit; byte-identical to e59d8d1)
          implementation_merge_sha: 97795a294733fec491207a75b6d7ccdd22cb161d   # squash merge onto main (1 parent ccce6d6)
          merge_status: merged
          merged_at: "2026-08-07T08:36:43Z"
          tree_identical_to_reviewed_head: true   # git diff 8c87583 97795a2 (and e59d8d1 97795a2) empty (byte-identical)
          ci_implementation: passed   # head 8c87583: Smoke + DB (PostgreSQL 16) both success
          ci_main_push: passed        # main push 97795a2: Smoke + DB (PostgreSQL 16) both success
          ci_note: transient_repo_wide_actions_stall_2026-08-06_13:49Z_to_21:24Z; empty retrigger commit 8c87583 (byte-identical) produced the clean run; no code changed to obtain the pass
          smoke_certification: passed_locally         # 32 suites, 5703 assertions, 0 failures (m27 smoke 57, conformance 3109)
          postgresql_certification: passed_locally     # 66 db specs, 2086 assertions, 0 failures (PostgreSQL 15.2 throwaway; m27-finance-ai 34 + m27-services 23 + all api-* green)
          lint: passed                                 # 0 errors (68 pre-existing baseline warnings; m27 adds none)
          certification_status: certified_on_branch    # cert PR pending merge
          certification_head: pending                  # docs-only cert commit
          certification_merge_sha: pending
          governed_mvp: partial
          local_reverification: {tables: 12, tables_force_rls: 12, tenant_isolation_policies: 12, delete_grants: 0,
                                 append_only_update_grants: 0, unsafe_tenant_fks: 0, float_columns: 0, secret_columns: 0,
                                 amount_minor_bigint: true, confidence_bps_integer: true, version_columns: 4,
                                 ai_permissions: 5, gov_check_constraints: 6, outboxes: 1, db_specs: 66, db_assertions: 2086,
                                 smoke_assertions: 5703, migrations: 52, audit_registry_total: 732, postgres_version: "15.2",
                                 clean_checkout: true}
          verdicts: {no_autopost: PASS, no_auto_match: PASS, explainability: PASS, money_safety: PASS, human_review: PASS,
                     m24_boundary: PASS, m15_m15a_boundary: PASS, m20_boundary: PASS, rls_tenancy: PASS, privacy: PASS,
                     idempotency_concurrency: PASS, permissions: PASS, audit: PASS, event_family_reuse: PASS,
                     api_absence: PASS, m24_m15_m20_unmodified: PASS, contamination: CLEAN}
          docs: [docs/05-ai/FINANCE_AI.md,
                 docs/build/stages/STAGE_5_M27_FINANCE_AI_ARCHITECTURE.md,
                 docs/build/stages/STAGE_5_M27_FINANCE_AI_READINESS.md,
                 docs/build/stages/STAGE_5_M27_FINANCE_AI_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_5_M27_FINANCE_AI_COMPLETION.md,
                 docs/build/stages/STAGE_5_M27_FINANCE_AI_CERTIFICATION.md]
          # certification_pr recorded once the docs-only cert PR is opened.
          stage_5_ai: m27_finance_certified_on_branch   # next: M28 Executive Copilot (m28-executive-ai) per BUILD_SEQUENCE
      - module: m28-executive-ai
        capability: Executive Copilot (read-only, cited)
        status: implemented   # Stage 5 M28 built (feature/stage-5-m28-executive-ai); governance/approve-m28-executive-ai (documented->approved_for_build) merged as gov PR #67 (main dceb5665b452b0bab10d24d5c109020ad3794b2c), then implemented. 7 owned copilot_* tables (4 mutable aggregates config/session/query/response + 3 append-only: citation/feedback/idempotency). GAP-4 RESOLVED (ADR-111): /api/v1/copilot reuses the SHARED ai.* namespace with 7 ai.copilot.* codes (4 privileged: export/sensitive/configure/platform); 9 AI_COPILOT_* audit codes (shared AI_ prefix; registered count 732->741). FIRST Stage-5 AI module WITH a REST surface (/api/v1/copilot); NO new event family, NO 2nd outbox (naming-map authoritative). Consumes the M24 governed pipeline BY CONTRACT via CopilotAiGatewayPort (M24CopilotGateway wraps m24 RequestService — routing/DLP/confidence/usage in m24, never duplicated/bypassed; opaque m24 request/output ids; NEVER auto-approves an m24 output). M32 UNBUILT -> DEFERRED behind read-only ExecutiveAnalyticsPort (deterministic fixture double + fail-closed UnavailableAnalyticsPort => review_required; m32 NOT built here); cross-domain reads via read-only ports with deterministic doubles (no network, no production provider). HARD RULES [read_only, cited, rls_masking] enforced in 5 layers: pure command/intent gate (evaluateReadOnlyGate) + prompt-injection screen (screenPromptInjection) + service authz (default deny) + API (no business-mutation route) + DB (copilot_query.read_only CHECK; copilot_config read_only/citations/export-review CHECKs always true; copilot_response_cited_ck completed=>cited; copilot_citation ref+granted CHECKs; app role has NO grant on any business table). RLS masking = tenant ∧ scope ∧ sensitivity ∧ required-entitlement intersection (evaluateEntitlement/maskEvidence); masked evidence dropped (no hidden-count leakage, no cross-tenant inference). Confidence integer bps; no float; no secret column; question/answer behind m09 refs. Deps m24 (certified). PG16-compatible. Spec docs/05-ai/EXECUTIVE_COPILOT.md; ADR-111, ADR-112.
        mvp: partial
        shared_services_consumed: [m24, m32]
        hard_rules: [read_only, cited, rls_masking]
        testing_required: [smoke, db_spec]
        implementation_5_m28:
          module: m28-executive-ai
          stage: "5"
          governance_pr: 67
          governance_merge_sha: dceb5665b452b0bab10d24d5c109020ad3794b2c   # main after governance PR #67 (m28 approved_for_build)
          branch: feature/stage-5-m28-executive-ai
          parent_baseline: dceb5665b452b0bab10d24d5c109020ad3794b2c
          gap4_resolution: {namespace: "ai.*", shared: true, adr: ADR-111, codes: [ai.copilot.read, ai.copilot.query, ai.copilot.feedback, ai.copilot.export, ai.copilot.sensitive, ai.copilot.configure, ai.copilot.platform], privileged: [ai.copilot.export, ai.copilot.sensitive, ai.copilot.configure, ai.copilot.platform]}
          m32_deferral: {port: ExecutiveAnalyticsPort, adr: ADR-112, built: false, double: FixtureAnalyticsPort, fail_closed: UnavailableAnalyticsPort}
          local_verification: {tables: 7, tables_force_rls: 7, tenant_isolation_policies: 7, composite_fks: 5, unsafe_tenant_fks: 0,
                               delete_grants: 0, append_only_ledgers: 3, gov_check_constraints: 32, float_columns: 0, secret_columns: 0,
                               confidence_bps_integer: true, version_columns: 4, ai_permissions: 7, privileged_permissions: 4,
                               audit_codes: 9, audit_registry_total: 741, event_families_new: 0, event_families_reused: 3, outboxes: 1,
                               http_routes: 14, mutating_routes: 6, business_mutation_routes: 0, migrations: 54,
                               smoke_suites: 33, smoke_assertions: 5824, db_specs: 69, db_assertions: 2167, postgres_version: "15.2"}
          verdicts: {read_only: PASS, m24_boundary: PASS, m32_deferred_port: PASS, cited: PASS, rls_masking: PASS,
                     entitlement_intersection: PASS, prompt_injection: PASS, confidence: PASS, human_review: PASS,
                     idempotency_concurrency: PASS, privacy: PASS, permissions: PASS, audit: PASS, event_family_reuse: PASS,
                     no_business_mutation_route: PASS, m24_m32_unmodified: PASS, contamination: CLEAN}
          docs: [docs/05-ai/EXECUTIVE_COPILOT.md,
                 docs/build/stages/STAGE_5_M28_EXECUTIVE_AI_ARCHITECTURE.md,
                 docs/build/stages/STAGE_5_M28_EXECUTIVE_AI_READINESS.md,
                 docs/build/stages/STAGE_5_M28_EXECUTIVE_AI_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_5_M28_EXECUTIVE_AI_COMPLETION.md]
          stage_5_ai: m28_executive_implemented_on_branch   # next per BUILD_SEQUENCE: M29 AI governance (m29-ai-governance)
        certification_5_m28:
          module: m28-executive-ai
          stage: "5"
          governance_pr: 67
          governance_merge_sha: dceb5665b452b0bab10d24d5c109020ad3794b2c
          implementation_pr: 68
          reviewed_implementation_head: 891fe66517c0326898bde585df72d2ee6fa0aa9d
          implementation_merge_sha: 47c4b79fa50ffd570730d4a174ecaa917cfd0142   # squash onto main (single parent dceb566)
          merge_status: merged
          merged_at: "2026-08-08T10:25:17Z"
          tree_identical_to_reviewed_head: true   # git diff 891fe66 47c4b79 EMPTY (byte-identical)
          parent_baseline: dceb5665b452b0bab10d24d5c109020ad3794b2c
          certification_baseline: 47c4b79fa50ffd570730d4a174ecaa917cfd0142   # merged main
          certification_branch: cert/stage-5-m28-executive-ai
          ci_implementation: passed   # reviewed head 891fe66: Smoke + DB (PostgreSQL 16) both success
          smoke_certification: passed_locally         # 33 suites, 5824 assertions, 0 failures (m28 83, conformance 3147)
          postgresql_certification: passed_locally     # 69 db specs, 2167 assertions, 0 failures (PostgreSQL 15.2 throwaway; m28-executive-ai 32 + m28-services 25 + api-copilot 24 + all api-* green)
          lint: passed                                 # 0 errors (68 pre-existing baseline warnings; m28 adds none)
          certification_status: certified_on_branch    # cert PR pending merge
          certification_head: pending                  # docs-only cert commit
          certification_merge_sha: pending
          gap4_resolution: {namespace: "ai.*", shared: true, adr: ADR-111, codes_total: 7, privileged: 4}
          m32_deferral: {port: ExecutiveAnalyticsPort, adr: ADR-112, built: false, fail_closed: UnavailableAnalyticsPort}
          local_reverification: {tables: 7, tables_force_rls: 7, tenant_isolation_policies: 7, composite_fks: 5, unsafe_tenant_fks: 0,
                                 delete_grants: 0, append_only_update_grants: 0, append_only_ledgers: 3, gov_check_constraints: 32,
                                 float_columns: 0, secret_columns: 0, confidence_bps_integer: true, version_columns: 4,
                                 ai_permissions: 7, privileged_permissions: 4, audit_codes: 9, audit_registry_total: 741,
                                 event_families_new: 0, event_families_reused: 3, outboxes: 1, http_routes: 14, mutating_routes: 6,
                                 business_mutation_routes: 0, migrations: 54, smoke_suites: 33, smoke_assertions: 5824,
                                 conformance_assertions: 3147, db_specs: 69, db_assertions: 2167, postgres_version: "15.2", clean_checkout: true}
          verdicts: {read_only: PASS, m24_boundary: PASS, m32_boundary: PASS, cited: PASS, rls_masking: PASS,
                     entitlement_intersection: PASS, prompt_injection: PASS, confidence: PASS, human_review: PASS,
                     idempotency_concurrency: PASS, privacy: PASS, permissions: PASS, audit: PASS, event_family_reuse: PASS,
                     no_business_mutation_route: PASS, m24_m32_unmodified: PASS, contamination: CLEAN}
          docs: [docs/build/stages/STAGE_5_M28_EXECUTIVE_AI_CERTIFICATION.md]
          stage_5_ai: m28_executive_certified_on_branch   # next per BUILD_SEQUENCE: M29 AI governance (m29-ai-governance)
      - module: m29-ai-governance
        capability: AI governance & release
        status: implemented   # Stage 5 M29 built (feature/stage-5-m29-ai-governance); governance/approve-m29-ai-governance (documented->approved_for_build) merged as gov PR #70 (main 667c7855e92bf129b950a964c7bc4594347d6e11), then implemented. LAST Stage-5 AI module. 7 owned ai_governance_* tables (3 mutable aggregates policy/use_case/release + 4 append-only: evaluation/decision/history/idempotency). THE LOAD-BEARING RULE: AI NEVER APPROVES ITS OWN RELEASE — enforced in 3 layers: pure gates (evaluateSodGate/evaluateReleaseGate/evaluateWaiverGate + isHumanActor: null/blank/system/ai/automation != human, proposer != approver) + service human-actor guards + DB CHECKs (ai_governance_release_human_ck approved=>approved_by NOT NULL; _sod_ck approved_by<>proposed_by; _evidence_ck non-waiver=>evaluation_passed; decision.decider NOT NULL). Policy can never disable human approval/evaluation nor blanket-allow restricted provider; use-case can never permit an AI-executed controlled action; ABSOLUTE controls never waivable (override blocked). 3 NEW privileged permissions ai.governance.approve/override/export (ai.governance.read/manage reused from m24); 16 AI_GOVERNANCE_* audit codes (shared AI_ prefix; count 741->757). NO REST API (api_prefixes []; internal governed library). EVENT OWNERSHIP RESOLVED (ADR-113): m24 OWNS the ai.governance_lifecycle family; m29 is an AUTHORIZED EMITTER reusing the existing GovernanceControlUpdated type via the ONE m06 outbox — NO new event family, NO schema fork, NO contracts/event-registry edit, NO 2nd outbox. Consumes M24 BY CONTRACT by OPAQUE id only (reads no m24/business table, calls no provider, stores no credential/secret, holds no prompt/output content). NO domain action, NO deployment/runtime control (records the governed decision + evidence and emits the event). Confidence/accuracy integer bps; no float; no secret column. Dep m24 (certified). PG16-compatible. ADR-113, ADR-114.
        mvp: false
        shared_services_consumed: [m24]
        testing_required: [smoke, db_spec]
        implementation_5_m29:
          module: m29-ai-governance
          stage: "5"
          governance_pr: 70
          governance_merge_sha: 667c7855e92bf129b950a964c7bc4594347d6e11
          branch: feature/stage-5-m29-ai-governance
          parent_baseline: 667c7855e92bf129b950a964c7bc4594347d6e11
          event_ownership: {family: ai.governance_lifecycle, owner: m24-ai-foundation, m29_role: authorized_emitter, reused_event_type: GovernanceControlUpdated, adr: ADR-113, new_family: false, schema_fork: false}
          no_ai_self_approval: {adr: ADR-114, layers: [pure_gates, service_guards, db_checks], db_checks: [ai_governance_release_human_ck, ai_governance_release_sod_ck, ai_governance_release_evidence_ck, ai_governance_decision_decider_not_null]}
          local_verification: {tables: 7, tables_force_rls: 7, tenant_isolation_policies: 7, composite_fks: 3, unsafe_tenant_fks: 0,
                               delete_grants: 0, append_only_ledgers: 4, gov_check_constraints: 26, float_columns: 0, secret_columns: 0,
                               confidence_bps_integer: true, version_columns: 3, new_permissions: 3, privileged_permissions: 3,
                               audit_codes: 16, audit_registry_total: 757, event_families_new: 0, event_families_reused: 1, outboxes: 1,
                               http_routes: 0, migrations: 56, smoke_suites: 34, smoke_assertions: 5932, conformance_assertions: 3186,
                               db_specs: 71, db_assertions: 2226, postgres_version: "15.2"}
          verdicts: {no_ai_self_approval: PASS, maker_checker_sod: PASS, evaluation_evidence_gate: PASS, policy_use_case_invariants: PASS,
                     waiver_override: PASS, absolute_control_not_waivable: PASS, m24_boundary: PASS, event_ownership: PASS,
                     idempotency_concurrency: PASS, tenancy: PASS, privacy: PASS, no_rest_api: PASS, no_runtime_deployment: PASS,
                     permissions: PASS, audit: PASS, event_family_reuse: PASS, m24_unmodified: PASS, contamination: CLEAN}
          docs: [docs/build/stages/STAGE_5_M29_AI_GOVERNANCE_ARCHITECTURE.md,
                 docs/build/stages/STAGE_5_M29_AI_GOVERNANCE_READINESS.md,
                 docs/build/stages/STAGE_5_M29_AI_GOVERNANCE_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_5_M29_AI_GOVERNANCE_COMPLETION.md]
          stage_5_ai: m29_ai_governance_implemented_on_branch   # Stage 5 AI (m24..m29) implementation COMPLETE on branch; next per BUILD_SEQUENCE is Stage 6 (enterprise platform) — NOT started
        certification_5_m29:
          module: m29-ai-governance
          stage: "5"
          governance_pr: 70
          governance_merge_sha: 667c7855e92bf129b950a964c7bc4594347d6e11
          implementation_pr: 71
          reviewed_implementation_head: 4841111c6ab231595ccd6dcb595c0392090940e4
          implementation_merge_sha: f00db24fa2c42ca9563f3146c97a9a52c00caad4   # squash onto main (single parent 667c785)
          merge_status: merged
          merged_at: "2026-08-08T11:30:17Z"
          tree_identical_to_reviewed_head: true   # git diff 4841111 f00db24 EMPTY (byte-identical)
          parent_baseline: 667c7855e92bf129b950a964c7bc4594347d6e11
          certification_baseline: f00db24fa2c42ca9563f3146c97a9a52c00caad4   # merged main
          certification_branch: cert/stage-5-m29-ai-governance
          ci_implementation: passed   # reviewed head 4841111: Smoke + DB (PostgreSQL 16) both success
          smoke_certification: passed_locally         # 34 suites, 5932 assertions, 0 failures (m29 69, conformance 3186)
          postgresql_certification: passed_locally     # 71 db specs, 2226 assertions, 0 failures (PostgreSQL 15.2 throwaway; m29-ai-governance 33 + m29-services 26 + all prior green)
          lint: passed                                 # 0 errors (68 pre-existing baseline warnings; m29 adds none)
          certification_status: certified_on_branch    # cert PR pending merge
          certification_head: pending                  # docs-only cert commit
          certification_merge_sha: pending
          event_ownership: {family: ai.governance_lifecycle, owner: m24-ai-foundation, m29_role: authorized_emitter, reused_event_type: GovernanceControlUpdated, adr: ADR-113, new_family: false, schema_fork: false, contracts_unchanged: true}
          no_ai_self_approval: {adr: ADR-114, layers: [pure_gates, service_guards, db_checks], verdict: PASS,
                                db_checks_present: [ai_governance_release_human_ck, ai_governance_release_sod_ck, ai_governance_release_evidence_ck, ai_governance_decision_decider_not_null, ai_governance_policy_human_ck, ai_governance_policy_eval_ck, ai_governance_policy_restricted_ck, ai_governance_use_case_noaction_ck]}
          local_reverification: {tables: 7, tables_force_rls: 7, tenant_isolation_policies: 7, composite_fks: 3, unsafe_tenant_fks: 0,
                                 delete_grants: 0, append_only_update_grants: 0, append_only_ledgers: 4, gov_check_constraints: 26,
                                 float_columns: 0, secret_columns: 0, confidence_bps_integer: true, version_columns: 3,
                                 new_permissions: 3, privileged_permissions: 3, audit_codes: 16, audit_registry_total: 757,
                                 event_families_new: 0, event_families_reused: 1, outboxes: 1, http_routes: 0, migrations: 56,
                                 smoke_suites: 34, smoke_assertions: 5932, conformance_assertions: 3186, db_specs: 71, db_assertions: 2226,
                                 postgres_version: "15.2", clean_checkout: true}
          verdicts: {no_ai_self_approval: PASS, maker_checker_sod: PASS, release_governance: PASS, evaluation_evidence: PASS,
                     use_case_governance: PASS, waiver_override: PASS, suspend_withdraw_supersede: PASS, m24_boundary: PASS,
                     event_ownership: PASS, permissions: PASS, audit: PASS, no_rest_api: PASS, no_runtime_deployment: PASS,
                     idempotency_concurrency: PASS, tenancy: PASS, privacy: PASS, m24_unmodified: PASS, contamination: CLEAN}
          docs: [docs/build/stages/STAGE_5_M29_AI_GOVERNANCE_CERTIFICATION.md]
          stage_5_ai: m29_ai_governance_certified_on_branch   # Stage 5 AI (m24..m29) COMPLETE + certified on branch; next per BUILD_SEQUENCE is Stage 6 (enterprise platform) — NOT started

  - stage: 6
    name: Enterprise platform (6A-6I)
    status: documented
    priority: P1_to_P2
    mvp: false
    depends_on_stage: 1
    modules:
      - module: m30-platform
        capability: Platform foundation (6A)
        status: implemented   # Stage 6A M30 built (feature/stage-6-m30-platform); governance/approve-m30-platform merged as gov PR #73 (main 3fc66fb8dfb5d312cd642ed1c24dd72e9e5d9fc1), then implemented. FIRST Stage-6 module. 10 owned platform_* tables (metadata + config definition/value/history + feature definition/assignment/history + secret_reference + secret_reference_history + idempotency; all tenant-scoped FORCE-RLS, mirroring m04-admin's model; a platform-scope row carries scope='platform' + requires the control-plane permission). GAP-5 RESOLVED (ADR-115): feature flags were never implemented — m04-admin is ORCHESTRATION-ONLY with no flag engine; M30 is the CANONICAL owner of the feature-flag engine (+ platform metadata/config/secret-reference seam); m04-admin CONSUMES by contract; NO duplicate engine. GAP-2 RESOLVED (ADR-115): platform.* namespace (9 codes; platform.control.administer is the cross-tenant control-plane permission a tenant admin never holds; 6 privileged). 11 PLATFORM_ audit codes (count 757->768). SECRETS SEAM (ADR-116): M30 owns the secret-reference SEAM only — opaque secretref: pointers (secretref: shape CHECK), ZERO secret VALUE columns (DB-proven); a secret-bearing config value is a reference XOR a plain value (platform_config_value_secret_ck); real secret/key management deferred to m41-security behind a fail-closed SecretResolver port (deterministic double only). HARD RULES: a feature flag is NEVER an authorization substitute (RBAC m02 authoritative — RBAC DENY + FEATURE ENABLED = DENY, tested); a platform-ABSOLUTE flag can never be weakened by a tenant override (evaluateAbsoluteAssignmentGate + PLATFORM_FEATURE_OVERRIDE_BLOCKED). DECLARES + OWNS the platform.lifecycle event family (contracts + event-registry) published through the ONE m06 outbox; NO 2nd outbox/engine. Consumes m01/m02/m03/m06 BY CONTRACT. NO REST API (api_prefixes []; internal governed library — ADR-116; m04-admin orchestrates over m30's contracts). reference_tables reconciled 26->10 (26 = full reference baseline; this foundation = governed core). No float; no secret value. Dep Stage 1 (certified). PG16-compatible. ADR-115, ADR-116.
        mvp: false
        testing_required: [smoke, db_spec]
        implementation_5_m30:
          module: m30-platform
          stage: "6A"
          governance_pr: 73
          governance_merge_sha: 3fc66fb8dfb5d312cd642ed1c24dd72e9e5d9fc1
          branch: feature/stage-6-m30-platform
          parent_baseline: 3fc66fb8dfb5d312cd642ed1c24dd72e9e5d9fc1
          gap5_resolution: {canonical_owner: m30-platform, m04_role: admin_console_consumer, adr: ADR-115, duplicate_engine: false, feature_flags_previously_implemented: false}
          gap2_resolution: {namespace: "platform.*", adr: ADR-115, codes_total: 9, privileged: 6, control_plane_permission: platform.control.administer}
          secrets_seam: {owner: m30-platform, scope: secret_references_only, adr: ADR-116, secret_value_columns: 0, real_key_mgmt: m41-security, resolver_port: SecretResolver, fail_closed_double: UnavailableSecretResolver}
          event_ownership: {family: platform.lifecycle, owner: m30-platform, outbox: m06, new_family: true, second_outbox: false}
          local_verification: {tables: 10, tables_force_rls: 10, tenant_isolation_policies: 10, composite_fks: 3, unsafe_tenant_fks: 0,
                               delete_grants: 0, append_only_ledgers: 4, gov_check_constraints: 25, float_columns: 0, secret_value_columns: 0,
                               version_columns: 6, permissions: 9, privileged_permissions: 6, audit_codes: 11, audit_registry_total: 768,
                               event_families_new: 1, outboxes: 1, http_routes: 0, migrations: 58, smoke_suites: 35, smoke_assertions: 6016,
                               conformance_assertions: 3227, db_specs: 73, db_assertions: 2281, postgres_version: "15.2"}
          verdicts: {feature_not_authz_substitute: PASS, absolute_control_not_overridable: PASS, secrets_seam_zero_values: PASS,
                     platform_vs_tenant_scope: PASS, gap5_no_duplicate_engine: PASS, gap2_permission_namespace: PASS, m24_secretref_convention: PASS,
                     event_ownership: PASS, idempotency_concurrency: PASS, tenancy: PASS, privacy: PASS, no_rest_api: PASS,
                     m04_unmodified: PASS, no_m41_secrets_engine: PASS, contamination: CLEAN}
          docs: [docs/03-platform/ENTERPRISE_PLATFORM_PHASE_6.md,
                 docs/build/stages/STAGE_6_M30_PLATFORM_ARCHITECTURE.md,
                 docs/build/stages/STAGE_6_M30_PLATFORM_READINESS.md,
                 docs/build/stages/STAGE_6_M30_PLATFORM_IMPLEMENTATION_PLAN.md,
                 docs/build/stages/STAGE_6_M30_PLATFORM_COMPLETION.md]
          stage_6: m30_platform_implemented_on_branch   # FIRST Stage-6 module built; next per BUILD_SEQUENCE is m31-studio (6B) — NOT started
        certification_6_m30:
          module: m30-platform
          stage: "6A"
          governance_pr: 73
          governance_merge_sha: 3fc66fb8dfb5d312cd642ed1c24dd72e9e5d9fc1
          implementation_pr: 74
          reviewed_implementation_head: 98c4331f7cb54792c678e223d5702df310ca9c96
          implementation_merge_sha: 2acb1700a8e89f3f9747cb1a7c152eb3dcdbfff8   # squash onto main (single parent 3fc66fb)
          merge_status: merged
          merged_at: "2026-08-08T12:59:22Z"
          tree_identical_to_reviewed_head: true   # git diff 98c4331 2acb170 EMPTY (byte-identical)
          parent_baseline: 3fc66fb8dfb5d312cd642ed1c24dd72e9e5d9fc1
          certification_baseline: 2acb1700a8e89f3f9747cb1a7c152eb3dcdbfff8   # merged main
          certification_branch: cert/stage-6-m30-platform
          ci_implementation: passed   # reviewed head 98c4331: Smoke + DB (PostgreSQL 16) both success
          smoke_certification: passed_locally         # 35 suites, 6016 assertions, 0 failures (m30 41, conformance 3227)
          postgresql_certification: passed_locally     # 73 db specs, 2281 assertions, 0 failures (PostgreSQL 15.2 throwaway; m30-platform 32 + m30-services 23 + all prior green)
          lint: passed                                 # 0 errors (68 pre-existing baseline warnings; m30 adds none)
          certification_status: certified_on_branch    # cert PR pending merge
          certification_head: pending                  # docs-only cert commit
          certification_merge_sha: pending
          gap5_verdict: PASS   # feature flags never implemented; m04-admin orchestration-only (0 feature tables, unmodified); m30 canonical owner; m04 consumes by contract; no duplicate engine (ADR-115)
          gap2_verdict: PASS   # platform.* namespace, 9 codes all 3-segment, 6 privileged, platform.control.administer control-plane, no platform.admin/wildcard, default deny (ADR-115)
          secret_seam_verdict: PASS   # 0 secret-value columns (opaque secret_ref pointers + secret_bearing flag only); secretref: shape CHECK; fail-closed SecretResolver double; real key mgmt = m41 (ADR-116)
          reference_table_reconciliation: {registry_baseline: 26, implemented_governed_core: 10, documented: true}
          local_reverification: {tables: 10, tables_force_rls: 10, tenant_isolation_policies: 10, composite_fks: 3, unsafe_tenant_fks: 0,
                                 delete_grants: 0, append_only_update_grants: 0, append_only_ledgers: 4, gov_check_constraints: 25,
                                 version_columns: 6, float_columns: 0, secret_value_columns: 0, permissions: 9, privileged_permissions: 6,
                                 audit_codes: 11, audit_registry_total: 768, event_families_new: 1, event_types: 8, outboxes: 1, http_routes: 0,
                                 migrations: 58, smoke_suites: 35, smoke_assertions: 6016, conformance_assertions: 3227, db_specs: 73,
                                 db_assertions: 2281, postgres_version: "15.2", clean_checkout: true}
          verdicts: {gap5_feature_ownership: PASS, gap2_permissions: PASS, secret_seam_zero_values: PASS, metadata_governance: PASS,
                     configuration_governance: PASS, feature_governance: PASS, feature_not_authz_substitute: PASS, absolute_control_override: PASS,
                     m04_boundary: PASS, m02_m03_m06_boundaries: PASS, event_ownership: PASS, audit: PASS, no_rest_api: PASS,
                     idempotency_concurrency: PASS, tenancy_platform_authority: PASS, privacy: PASS, contamination: CLEAN}
          docs: [docs/build/stages/STAGE_6_M30_PLATFORM_CERTIFICATION.md]
          stage_6: m30_platform_certified_on_branch   # FIRST Stage-6 module certified on branch; next per BUILD_SEQUENCE is m31-studio (6B) — NOT started
      # m31-studio APPROVED FOR BUILD (Stage 6B governance, 2026-08-08). Studio is the design-time AUTHORING/publish
      # layer only; it MUST NOT become a second runtime engine. Canonical runtime owners stay: m06 workflow (already owns
      # workflow_definition/_version + workflow.definition.* authoring/publish + execution), m07 rules (owns
      # rule_set/_version + rules.engine.author/publish + evaluation), m08 notifications, m30 config/feature-flags/secret-
      # reference seam. Studio CONSUMES/BINDS these by contract (like m04-admin orchestrates) — no duplicate
      # workflow_definition or rule_set table, no second outbox (reuse m06), studio.lifecycle = DESIGN-TIME events only.
      # Reviewer gaps to resolve at build via ADR + SHARED_SERVICE_OWNERSHIP (non-blocking, precedent m30 GAP-2/GAP-5):
      #   G-a PERMISSION NAMESPACE: naming-map permission_namespaces:[] (same shape as m30 GAP-2). Declare studio.* (3-seg,
      #       default-deny) so every controlled design/validate/publish/bind op authorizes a permission; no studio.admin wildcard.
      #   G-b FORMS OWNERSHIP: no canonical form/schema capability exists anywhere (only m12 feedback_questionnaire, a feedback
      #       table). m31 MAY become the canonical owner of reusable form/schema definitions — record in SHARED_SERVICE_OWNERSHIP + ADR.
      #   G-c DESIGN-vs-RUNTIME SECOND-ENGINE risk: m06/m07 already own their own definition authoring+publish. ADR must fix the
      #       boundary (Studio authors design-artifacts that bind to m06/m07 definitions; it does not re-implement them).
      #   G-d HIGH-RISK EXECUTION SURFACE: NO arbitrary code / expression / SQL / shell execution. Reuse m06's sandboxed expression
      #       evaluation + m07's rule engine; registered capabilities/extension points only (per Phase-6 guarantee). Fail closed.
      #   G-e INTEGRATION BOUNDARY: m33-integration is canonical + UNBUILT — connector/action nodes are opaque references only,
      #       deferred behind a port until m33 (port-first, precedent m28→m32, m30→m41). Do NOT build m33.
      #   G-f SECRETS: any secret-bearing design value carries an m30 secretref: pointer only; never inline a secret in a definition.
      # Maker-checker: publish/release of a Studio artifact is a controlled action (approver != author). Deps m02/m03/m06/m07/m08/
      # m09/m30 all implemented+certified on main; no unbuilt HARD dep (m33 deferred behind port). GOVERNANCE-ONLY: status flip only.
      # IMPLEMENTED (Stage 6B, 2026-08-08). All reviewer gaps above resolved: G-a studio.* namespace (ADR-118, 9 codes);
      # G-b forms canonical ownership assigned (ADR-117 + SHARED_SERVICE_OWNERSHIP); G-c design-vs-runtime boundary
      # (ADR-117 — binds to m06/m07, no duplicate engine/table); G-d no arbitrary code (reuses m06 sandbox + m07
      # conditions, prohibited-expression + secret scan, fail closed); G-e m33 deferred behind a fail-closed port;
      # G-f secretref-only. Publish is a controlled action (maker-checker/SoD, human approver); published version immutable.
      - module: m31-studio
        capability: Workflow/BPM/Forms/Rules Studio (6B)
        status: implemented
        mvp: false
        testing_required: [smoke, db_spec]
        implementation_6_m31:
          adrs: [ADR-117, ADR-118]
          tables: 9
          reconciliation: "reference_tables 25 (full reference baseline) -> 9 governed tables; design-time layer binds to canonical m06/m07 runtime"
          permission_namespace: "studio.* (9 codes, 5 privileged, all 3-segment)"
          audit_prefix: STUDIO_
          audit_codes: 14
          event_family: studio.lifecycle
          event_types: 6
          outboxes: 0
          api_prefixes: []
          second_engine: none
          secret_value_columns: 0
          floats: 0
          binds_to: [m06-workflow, m07-rules]
          integration_boundary: "m33 deferred behind fail-closed IntegrationCapabilityCatalogPort"
          branch: feature/stage-6-m31-studio
        certification_6_m31:
          verdict: certified_on_branch
          date: "2026-08-10"
          governance_pr: 76
          governance_merge_sha: e0acc872e6bef14f2cac06570e64cfb5428ec18e
          implementation_pr: 77
          reviewed_head: 542e4e8
          implementation_merge_sha: ffdcbcad0bf94171aec66449e2dd7ae08b703931
          tree_equivalence: "git diff 542e4e8 ffdcbca == EMPTY (byte-identical squash, single parent)"
          certification_baseline: ffdcbcad0bf94171aec66449e2dd7ae08b703931
          certification_branch: cert/stage-6-m31-studio
          implementation_ci: "Smoke success + DB PG16 success on 542e4e8"
          local_postgres: "15.2 throwaway (authoritative CI = PG16; no PG16 claimed locally)"
          gates: "format clean; lint 0-err (68 baseline warnings); build clean; smoke 36/6157/0-fail (m31 92, conformance 3274); migrate 60; DB 75/2332/0-fail (m31-studio 32 + m31-services 19)"
          metrics: "9 tables/9 FORCE-RLS/9 policies/7 composite FKs/0 unsafe/0 DELETE/6 append-only(0 UPDATE)/21 CHECKs/3 version cols/0 float/0 secret-value/0 submission tables/1 immutability trigger/9 perms(5 priv)/14 STUDIO_ codes(reg 782)/studio.lifecycle 6 types/1 outbox(m06)/0 routes"
          contamination: clean
          report: docs/build/stages/STAGE_6_M31_STUDIO_CERTIFICATION.md
      # m32-analytics APPROVED FOR BUILD (Stage 6C governance, 2026-08-10). Enterprise reporting/analytics BUILDER:
      # semantic datasets, metrics/KPIs, reports, dashboards, RLS + masking, exports. It is a DERIVED/READ layer over the
      # operational modules — SOURCE OF TRUTH stays with each source module; analytics copies are derived/read-only/
      # rebuildable, and NO operational mutation happens through m32. Canonical owners stay: m02 RBAC, m03 audit, m06
      # workflow/timers/OUTBOX, m08 notifications, m09 documents, m30 platform/config/feature-flags/secret-seam, m24 AI,
      # m04 admin saved-views. m32 declares /api/v1/analytics + analytics.* namespace + analytics.lifecycle (registered) +
      # ANALYTICS_ audit. Only HARD dep = m30 (certified on main); source read-modules (m12/m13/m14/m16/m17/m19/m15/m20/
      # m21/m22/m24/m28/m30/m31) all implemented+certified and consumed BY READ CONTRACT/PORT (deterministic doubles until
      # each real adapter is wired, m28 precedent). No unbuilt HARD dep (m33+ NOT required).
      # Reviewer gaps to resolve at build via ADR + SHARED_SERVICE_OWNERSHIP (non-blocking, m30/m31 precedent):
      #   G-a HIGH-RISK QUERY ENGINE: NO arbitrary tenant SQL / eval / shell / unrestricted table access. Expose a GOVERNED
      #       semantic layer only (predefined metrics, bounded dimensions, whitelisted operators, parameterized queries);
      #       compile safely; fail closed. This is the load-bearing analytics control.
      #   G-b RLS / ENTITLEMENT PRESERVATION: aggregation can leak hidden data. Every tenant table FORCE-RLS; derived
      #       analytics MUST preserve source row entitlements + sensitivity masking + ethical walls; no hidden-count leak,
      #       no cross-tenant aggregate inference; a user gains NO access merely because data is aggregated.
      #   G-c REPORTING OWNERSHIP overlap: SHARED_SERVICE_OWNERSHIP co-lists m10-report (Stage 2, mvp:true, 0 tables, unbuilt)
      #       + m32-analytics. Resolve canonical ownership via ADR (m32 = enterprise analytics/reporting builder; no 2nd engine).
      #   G-d SAVED-VIEW/DASHBOARD overlap: m04 owns admin_saved_view (console). m32 dashboards/saved-analytics-views are
      #       analytics-scoped — do NOT create a 2nd generic saved-view engine; clarify the boundary.
      #   G-e M28 PORT: m32 is the real implementation of M28 ExecutiveAnalyticsPort (ADR-112) — read-only, bounded,
      #       tenant-scoped, entitlement-filtered, citation/lineage-bearing. Preserve those semantics; M28 = downstream consumer.
      #   G-f MATERIALIZATION + LINEAGE: if m32 materializes projections/aggregates, they are derived/read-only/rebuildable
      #       with lineage (source module, opaque ref, metric/report version, extraction ts, window, filters, classification);
      #       source module stays authoritative. No fabricated analytics source (M28 cites this evidence).
      #   G-g SCHEDULER/NOTIFY reuse: scheduled reports reuse m06 timers + m08 notifications; NO 2nd scheduler/timer/notify
      #       engine; m32 owns only schedule metadata. Publish via the ONE m06 outbox; no 2nd outbox.
      #   G-h EXPORTS: explicit analytics.*.export permission; tenant/row filter BEFORE export; sensitive export privileged;
      #       audit every export; bounded size; no cross-tenant/hidden-row export; no secrets.
      #   G-i MONEY/NUMERIC: no float money; bigint minor units; exact decimal for ratios; integer bps; explicit currency;
      #       no silent cross-currency aggregation (analytics must not reintroduce float finance deliberately avoided).
      #   G-j 42 reference_tables -> governed core reconciliation (m30 26->10, m31 25->9 precedent); document honestly.
      # Maker-checker: metric/report PUBLICATION that drives executive/M28 reporting is a controlled action (assess approver!=author).
      # IMPLEMENTED (Stage 6C, 2026-08-10). All reviewer gaps above resolved: G-a governed semantic query (compileMetricQuery,
      # whitelisted dims/measures/operators, no arbitrary SQL, fail closed); G-b entitlement intersection on aggregates
      # (evaluateEntitlement — aggregation grants no access); G-c m10-report ownership (ADR-119, m32 canonical); G-d m04
      # admin_saved_view boundary (m32 owns analytics dashboards, no 2nd saved-view engine); G-e M28 ExecutiveAnalyticsPort
      # implemented (M32ExecutiveAnalyticsAdapter); G-f materialization+lineage (append-only rebuildable, analytics_lineage
      # mandatory); G-g scheduler/notify reuse (opaque m06 timer + m08 notify refs, no engine); G-h exports (privileged,
      # filter-before-export, audited, bounded); G-i money (bigint minor/numeric decimal/bps, 0 float); G-j 42->11 core.
      - module: m32-analytics
        capability: Reporting & analytics builder (6C)
        status: implemented
        mvp: false
        testing_required: [smoke, db_spec]
        implementation_6_m32:
          adrs: [ADR-119]
          tables: 11
          reconciliation: "reference_tables 42 (full reference baseline) -> 11 governed tables; derived/read analytics layer over source modules"
          permission_namespace: "analytics.* (12 codes, 6 privileged, all 3-segment)"
          audit_prefix: ANALYTICS_
          audit_codes: 17
          event_family: analytics.lifecycle
          event_types: 7
          outboxes: 0
          api_prefixes: [/api/v1/analytics]
          second_engine: none
          secret_value_columns: 0
          floats: 0
          binds_to: [m28-executive-ai, m30-platform]
          consumers: [m28-executive-ai]
          query_engine: "governed semantic (whitelisted dims/measures/operators, parameterized, no arbitrary SQL)"
          integration_boundary: "m33/m41 deferred behind fail-closed MaterializationSourcePort"
          branch: feature/stage-6-m32-analytics
        certification_6_m32:
          verdict: certified_on_branch
          date: "2026-08-10"
          governance_pr: 79
          governance_merge_sha: a27be3a9ace6fdf3e12b6782b3b866676e2cdb83
          implementation_pr: 80
          reviewed_head: d8b0d08
          implementation_merge_sha: 3680779f4c7e2a0537e2092cff57d0974a6ebccf
          tree_equivalence: "git diff d8b0d08 3680779 == EMPTY (byte-identical squash, single parent)"
          certification_baseline: 3680779f4c7e2a0537e2092cff57d0974a6ebccf
          certification_branch: cert/stage-6-m32-analytics
          implementation_ci: "Smoke success + DB PG16 success on d8b0d08"
          local_postgres: "15.2 throwaway (authoritative CI = PG16; no PG16 claimed locally)"
          gates: "format clean; lint 0-err (68 baseline warnings); build clean; smoke 37/6319/0-fail (m32 99, conformance 3335); migrate 62; DB 77/2389/0-fail (m32-analytics 34 + m32-services 23)"
          metrics: "11 tables/11 FORCE-RLS/11 policies/7 composite FKs/0 unsafe/0 DELETE/5 append-only/43 CHECKs/6 version cols/0 float/0 secret-value/2 immutability triggers/12 perms(6 priv)/17 ANALYTICS_ codes(reg 799)/analytics.lifecycle 7 types/1 outbox(m06)"
          boundaries: "M28 port implemented + m28 source untouched (downstream consumer); m10 subsumed; m04 saved-view clean; m06 timer + m08 notify reused (opaque refs); one m06 outbox"
          adapter_limitation: "M32ExecutiveAnalyticsAdapter exported but not swapped into copilot module (fixture retained; m28 boundary intact)"
          contamination: clean
          report: docs/build/stages/STAGE_6_M32_ANALYTICS_CERTIFICATION.md
      # m33-integration APPROVED FOR BUILD (Stage 6D-1 governance, 2026-08-10). Integration FOUNDATION: connector SDK/
      # registry, connection management, connector runtime — the canonical integration seam the deferred ports in m31
      # (IntegrationCapabilityCatalogPort) + m32 (MaterializationSourcePort) resolve to. Declares /api/v1/integration +
      # integration.* namespace + connector.lifecycle (registered) + INTEGRATION_ audit. Only HARD dep = m30 (certified on
      # main); MODULE_DEPENDENCY_MAP m33->m30. No unbuilt HARD dep (m34-m37 are DOWNSTREAM; m38-automation depends on m33).
      # Reviewer gaps to resolve at build via ADR + SHARED_SERVICE_OWNERSHIP (non-blocking, m30/m31/m32 precedent):
      #   G-a HIGH-RISK CONNECTOR RUNTIME / untested integrations: per CLAUDE.md connectors ship Framework Only / Sandbox
      #       Ready / Production Ready Pending Credentials until proven — NO production network egress, deterministic offline
      #       doubles + fail-closed, honest connector status; never claim untested integrations production-ready.
      #   G-b HIGH-RISK "connection + SECRET MANAGER" — NO second secrets manager: connection configs store OPAQUE secretref:
      #       pointers (m30 seam) ONLY; ZERO credential/secret VALUE columns (DB-proven); real credential resolution deferred
      #       to m41-security behind a fail-closed SecretResolver port (ADR-116 precedent). m33 is NOT the secrets manager.
      #   G-c TABLE-NAMESPACE COLLISION: the integration_* table prefix is ALREADY owned by m23-finance-integration (Stage 3,
      #       finance-specific, implemented). m33 (platform foundation) MUST use a DISTINCT prefix (e.g. connector_*, aligned
      #       with the connector.lifecycle family) to avoid collision. Resolve at build.
      #   G-d CONNECTOR SDK = REGISTERED CAPABILITIES, no arbitrary code (Phase-6 guarantee): no arbitrary code / eval / shell
      #       / direct DB access to other modules' tables / approval-SoD bypass; sandboxed; registered connectors only.
      #   G-e DEFERRED-PORT OWNER: m33 is the canonical integration foundation m31/m32 (and later m38) resolve to; provide
      #       adapters preserving their fail-closed/read-only semantics — do NOT rewire m28/m31/m32 beyond wiring adapters.
      #   G-f EVENT/OUTBOX: m33 owns connector.lifecycle ONLY (marketplace/devportal/webhook/eventstream/govrelease belong to
      #       m34-m37); publish via the ONE m06 outbox; NO second outbox/event-delivery path (webhooks/eventstream = m36, deferred).
      #   G-g TENANCY: connection/connector-install tables tenant-scoped FORCE-RLS + composite tenant-safe FKs; no cross-tenant
      #       connection access. Idempotency for connector runs / event ingestion (high-risk actions).
      #   G-h 38 reference_tables -> governed core reconciliation (m30 26->10, m31 25->9, m32 42->11 precedent); document honestly.
      # Maker-checker: connector installation/publication that grants external data access is a controlled action (assess approver!=author).
      # IMPLEMENTED (Stage 6D-1, 2026-08-10). All reviewer gaps above resolved: G-a framework-only runtime (fail-closed
      # ConnectorRuntimePort, deterministic doubles, no production egress); G-b no second secrets manager (connection_secret
      # secretref-only, 0 credential-value cols, real key mgmt=m41 behind fail-closed resolver); G-c connector_* prefix (not
      # m23's integration_*); G-d registered capabilities/no arbitrary code; G-e implements m31 IntegrationCapabilityCatalogPort
      # (M33IntegrationCapabilityCatalog; m31 unchanged); G-f connector.lifecycle only + one m06 outbox; G-g FORCE-RLS +
      # composite FKs + idempotency; G-h 38->9 core. Connector publish = maker-checker/SoD, published-immutable.
      - module: m33-integration
        capability: Integration foundation (6D-1)
        status: implemented
        mvp: false
        testing_required: [smoke, db_spec]
        implementation_6_m33:
          adrs: [ADR-120]
          tables: 9
          table_prefix: connector_
          reconciliation: "reference_tables 38 (full reference baseline) -> 9 governed connector_* tables; framework-only foundation"
          permission_namespace: "integration.* (9 codes, 4 privileged, all 3-segment)"
          audit_prefix: INTEGRATION_
          audit_codes: 15
          event_family: connector.lifecycle
          event_types: 6
          outboxes: 0
          api_prefixes: [/api/v1/integration]
          second_engine: none
          secret_value_columns: 0
          floats: 0
          runtime: "framework-only, fail-closed ConnectorRuntimePort (no production egress)"
          implements_port: "m31 IntegrationCapabilityCatalogPort"
          binds_to: [m30-platform, m31-studio]
          secrets: "connection_secret secretref-only (m30 seam); real key mgmt = m41 deferred"
          branch: feature/stage-6-m33-integration
      - {module: m34-marketplace, capability: Connector marketplace (6D-2), status: documented, mvp: false, testing_required: [smoke, db_spec]}
      - {module: m35-devportal, capability: Public APIs + developer portal (6D-3), status: documented, mvp: false, testing_required: [smoke, db_spec]}
      - {module: m36-events, capability: Webhooks + event streaming (6D-4), status: documented, mvp: false, testing_required: [smoke, db_spec]}
      - {module: m37-govrelease, capability: Integration governance/QA/release (6D-5), status: documented, mvp: false, testing_required: [smoke, db_spec]}
      - {module: m38-automation, capability: Scheduler/automation/extensions (6E), status: documented, mvp: false, testing_required: [smoke, db_spec]}
      - {module: m39-saas, capability: Tenant admin/billing/white-label/commercial SaaS (6F), status: deferred, mvp: false, requires_review: [billing_provider, plan_taxonomy], testing_required: [smoke, db_spec]}
      - {module: m40-resilience, capability: Mobile/offline/observability/backup/BC (6G), status: documented, mvp: false, testing_required: [smoke, db_spec]}
      - {module: m41-security, capability: Enterprise security/privacy/compliance/GRC (6H), status: documented, mvp: false, testing_required: [smoke, db_spec]}
      - {module: m42-certification, capability: Enterprise integration/certification/release (6I), status: documented, mvp: false, testing_required: [smoke, db_spec]}

  - stage: 7
    name: Hardening (CONDITIONAL-GO conditions)
    status: requires_review
    priority: P0_for_GA
    mvp: false
    depends_on_stage: 6
    items:
      - {item: penetration_test, status: requires_review}
      - {item: dr_failover_failback_drill, status: requires_review}
      - {item: load_and_chaos_at_scale, status: requires_review}
      - {item: real_data_migration_execution, status: requires_review, requires: [finance_signoff, legal_signoff]}

  - stage: 8
    name: Phase 7 — Vertical business solutions
    status: deferred
    priority: P2
    mvp: false
    depends_on_stage: 7
    note: Begins only after a live, stable platform meets the Phase 7 entry criteria.
